// This is the main Prisma schema entrypoint.
// Domain enums and models live under prisma/models/.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}
