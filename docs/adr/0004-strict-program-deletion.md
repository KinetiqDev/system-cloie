# Strict Program Deletion

Programs may be permanently deleted only by a Secretary or Dean after they are inactive and have no linked records. Every direct program relationship uses database `RESTRICT` protection, while deactivation remains the reversible lifecycle option; this prevents permanent deletion from erasing history or silently unlinking records.
