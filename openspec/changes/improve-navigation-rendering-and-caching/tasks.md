## Required Implementation And Verification Skills

- `next-best-practices` is required for route boundaries, Server Components, Server Actions, route handlers, dynamic imports, and persistent Next.js cache work.
- `shadcn` is required before modifying loading skeletons, Base UI dialogs or drawers, navigation controls, filter selects, badges, or chart surfaces. Inspect installed components and current registry documentation/examples before changing composition; retain Base UI compatibility and do not add Radix packages.
- `chrome-devtools` is required for browser-evidence tasks.
- `a11y-debugging` is required for tasks 1.3, 2.2, 2.3, and 4.3.
- `debug-optimize-lcp` is required for tasks 1.1, 1.2, 1.3, 3.2, 4.3, 5.2, and 7.2.
- `codebase-design` is required for tasks 5.1 and 6.1 through 6.3 to define the authorized Dean read-service seam and cache-ownership interface.
- `troubleshooting` is conditional and is used only when Chrome DevTools MCP connection, target selection, or server initialization fails.
- `memory-leak-debugging` is conditional and is used only after repeated navigation, drawer, or visualization interaction shows sustained heap growth. It is not a completion gate for this change.

## 1. Baseline and Rendering Foundation

- [ ] 1.1 Establish the browser-evidence authentication contract and record production-build baselines for Secretary Course Assignments, Dean Dashboard, and Faculty Dashboard. Scope: disposable real Supabase-authenticated test accounts or a separately reviewed test-only mechanism; audit evidence only. This task blocks any acceptance criterion requiring an authenticated production-build browser trace. Acceptance: each record identifies environment, role, account state, route, viewport, Fast 3G plus 4x CPU throttle, and safe authentication setup; it distinguishes server-rendered initial data from post-hydration reads, identifies chart chunks, and records LCP element and breakdown. Verify: `pnpm build`, Chrome DevTools `emulate`, `performance_start_trace`, `performance_analyze_insight`, `list_network_requests`, `get_network_request`, `pnpm lint`. Commit: `test(perf): record role route rendering baselines`.
- [ ] 1.2 Introduce an authenticated-shell Suspense boundary that preserves server-side session, profile-gate, and role authorization while displaying a role-neutral shell fallback without protected content. Scope: authenticated route entry only. Paths: `src/app/(app)/layout.tsx`, `src/features/auth/components/session-guard.tsx`, `src/components/layout/app-shell.tsx`, new shell fallback component and focused tests. Acceptance: unauthenticated, inactive, rejected, incomplete, and wrong-role requests retain their current redirect/status behavior; authorized routes show a stable fallback during delayed session resolution; a performance trace proves shell fallback renders before delayed content. Verify: focused SessionGuard/layout tests, Chrome DevTools performance trace, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(rendering): stream authenticated application shell`.
- [ ] 1.3 Add role-scoped error recovery and meaningful loading geometry for selected high-traffic routes. Scope: a route inventory table records each selected route, fallback owner, geometry source, and whether it is route-level or local; for Dean, add only role-level recovery and route-segment geometry because task 5.2 owns local Dashboard and College Oversight boundaries. Paths: selected files under `src/app/(app)/{dean,secretary,program-head,faculty,student,alumni,industry-partner}/`, shared skeleton components only where identical geometry is reused, route tests. Acceptance: async route transitions display route-appropriate skeletons; errors below role boundaries retain the application shell, expose an accessible retry action, and reveal no internal exception text; performance traces prove only intended regions suspend. Verify: focused route/component tests, a11y-debugging, Chrome DevTools traces, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(rendering): add role route loading and recovery boundaries`.

## 2. Navigation Accessibility and Feedback

- [ ] 2.1 Unify desktop, drawer, and bottom-navigation active matching on a normalized deepest-match navigation selection and expose `aria-current="page"` only for that destination. Scope: navigation semantics only. Paths: `src/components/layout/sidebar.tsx`, `mobile-nav.tsx`, `mobile-sidebar-drawer.tsx`, `src/lib/constants/navigation.ts`, navigation tests. Acceptance: nested routes expose exactly one current-page link per visible surface; ancestor groups remain expanded or contextual without claiming current-page status; unrelated routes do not; no role URL or navigation source duplication is introduced. Verify: focused navigation/component tests covering root, group landing, nested Dean child, non-Dean detail, and trailing slash normalization; `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(navigation): align active role route state`.
- [ ] 2.2 Add subtle local pending feedback for slow role navigation and correct accessible names for Course Assignment filter controls while preserving route-level loading UI as the primary feedback. Scope: navigation/link state and filter labeling only. Paths: `src/components/layout/**`, `src/features/course-assignments/components/shared/assignment-filters.tsx`, focused component tests. Acceptance: a pending destination is identifiable without disabling unrelated controls or showing a global spinner; every select-like filter has a visible or programmatic name. Verify: focused accessibility/component tests, a11y-debugging, Lighthouse snapshot, and Chrome DevTools console issue inspection for Course Assignments; `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(navigation): expose pending and labeled controls`.
- [ ] 2.3 Resolve audited responsive navigation defects without changing Dean group-landing URLs: verify drawer focus containment, remove inconsistent mobile active semantics, and address the Dean Dashboard status-badge contrast failure and logo sizing warning. Scope: layout accessibility and styling only. Paths: `src/components/layout/**`, relevant badge/logo consumers, component tests. Acceptance: drawer focus cycles within all interactive drawer elements, Escape returns focus to the trigger, active state agrees across visible navigation surfaces, Lighthouse reports no audited contrast failure, and browser console has no logo aspect-ratio warning. Verify: focused drawer tests; a11y-debugging keyboard `Tab` and `Shift+Tab` snapshots, Escape, trigger focus restoration, and accessibility-tree checks; Lighthouse, console issue inspection, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(a11y): polish responsive role navigation`.

## 3. Server-First Course Assignment Lists

- [ ] 3.1 Define and test canonical URL parsing/serialization for Course Assignment filters and pagination using the design query table. Scope: role-owned list state. Paths: `src/features/course-assignments/**`, `src/app/(app)/{secretary,dean,program-head}/**/course-assignments/**`, focused route/service tests. Acceptance: all URL input is parsed server-side through one schema; the validated state is the only list-service input; malformed, repeated, unknown, out-of-range, and canonical-equivalent values are normalized; Program Head scope is derived from the validated Program Head assignment and never trusted from a URL program identifier. Verify: parameterized focused route/service tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(course-assignments): define route list state`.
- [ ] 3.2 Render initial Secretary, Dean, and Program Head Course Assignment records in authorized Server Components and narrow the client shell to filters, dialogs, mutations, and local interaction state. Scope: initial table rendering. Paths: `src/app/(app)/secretary/course-assignments/page.tsx`, `src/app/(app)/dean/academic-structure/course-assignments/page.tsx`, `src/app/(app)/program-head/course-assignments/page.tsx`, `src/features/course-assignments/components/course-assignments-page-shell.tsx`, `course-assignments-table.tsx`, services/actions/tests. Acceptance: first records are present in the route response; no mount-time Server Action is required to fetch the initial table; post-mutation refresh preserves validated URL state; Program Head program scope and General Education rules remain enforced server-side. Verify: focused route/component/action tests, production-authenticated Fast 3G/4x CPU Chrome network and LCP trace, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(course-assignments): server render initial role lists`.
- [ ] 3.3 Remove superseded initial-read Server Action paths after server-first parity is proven, while retaining mutation actions and targeted refresh behavior. Scope: dead client read code only. Paths: `src/features/course-assignments/components/**`, `src/lib/actions/course-assignment-actions.ts`, related tests. Acceptance: no unused client mount fetch remains; create/edit/delete flows refresh the correct role-owned routes; existing authorization and validation remain unchanged. Verify: focused Course Assignment tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(course-assignments): remove hydration read action`.

## 4. Bounded and Parallel Read Models

- [ ] 4.1 Move Secretary Users pagination, filtering, sorting, and field selection to Prisma queries and retain KPI/program filter behavior without serializing every user to the browser. Scope: Secretary Users list. Paths: `src/app/(app)/secretary/users/page.tsx`, `src/features/users/services/list-secretary-users-summary.ts`, `src/features/users/components/secretary-users-list/**`, tests. Acceptance: a requested page returns only requested user records and required display fields; total/KPI/filter outputs remain correct; server-side URL sorting/filtering uses an explicit allowlist and page boundary behavior is tested. Verify: focused service/component/route tests and representative browser check, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(users): paginate secretary user reads in database`.
- [ ] 4.2 Replace Student course-bound evaluation eligibility per-assignment reads with a bounded batch or set-based projection while preserving every existing eligibility reason and exclusion rule. Scope: Student assigned evaluation read model. Paths: `src/features/responses/services/list-student-assigned-evaluations.ts`, eligibility helpers/tests, disposable-DB integration test if query equivalence needs database proof. Acceptance: multiple course-bound assignments do not produce one independent eligibility query each; active/submitted visibility and access behavior match the current rules. Verify: focused unit tests, `RUN_DATABASE_INTEGRATION_TESTS=1 pnpm test:db` only against a disposable database when applicable, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(responses): batch student evaluation eligibility`.
- [ ] 4.3 Parallelize Faculty Dashboard independent reads, pass validated request scope through dependent services where safe, and defer below-the-fold charts and word cloud with accessible geometry-preserving fallbacks. Scope: Faculty Dashboard. Paths: `src/app/(app)/faculty/dashboard/page.tsx`, `src/features/analytics/services/get-faculty-dashboard.ts`, `src/features/analytics/components/**`, dashboard tests. Acceptance: KPI values and authorization are unchanged; independent reads no longer wait sequentially; heading/KPI content is usable before Recharts and word-cloud chunks finish; chart and word-cloud props contain only approved aggregate or de-identified fields and exclude respondent identifiers and raw qualitative content. Verify: focused service/route/serialization tests, a11y-debugging fallback checks, production-authenticated chunk and LCP trace, Lighthouse snapshot, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(faculty): stream dashboard metrics before charts`.
- [ ] 4.4 Resolve Program Head assignment scope once per request and reuse it across dashboard reads; reuse request-snapshot verification status for Alumni and Industry Partner layouts when correctness permits. Scope: request-scoped duplicate reads only. Paths: `src/app/(app)/program-head/dashboard/page.tsx`, `src/features/analytics/services/get-program-head-dashboard.ts`, `src/app/(app)/{alumni,industry-partner}/layout.tsx`, auth snapshot/types/tests. Acceptance: no duplicate Program Head assignment lookup or unnecessary external-profile verification lookup remains; role, account-state, and verification outcomes are unchanged. Verify: focused auth/dashboard tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(auth): reuse request scoped role data`.

## 5. Dean Read Model and Route Streaming

- [ ] 5.1 Extract a small shared authorized Dean read-service interface and update Dean Server Component pages to call it directly while API route handlers retain independent Dean authorization. Scope: Dean Dashboard and College Oversight routes. Paths: `src/app/(app)/dean/dashboard/page.tsx`, `src/app/(app)/dean/college-oversight/**/page.tsx`, `src/features/dean/services/read-dean-oversight.ts`, `fetch-dean-read.ts`, `src/app/api/dean/**`, tests. Acceptance: Server Components no longer invoke route handlers through synthetic Request/Response JSON; direct service calls and API routes deny non-Dean access equivalently; not-found and bad-request mapping remain intact. Verify: codebase-design seam review, focused Dean page/API/authorization tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(dean): call oversight read models directly`.
- [ ] 5.2 Split Dean Dashboard and College Oversight reads into independent authorized async regions with local fallbacks, preserving query parameter validation and canonical redirects. Scope: Dean local streaming behavior after tasks 1.1 and 1.3 provide production browser evidence, role-level recovery, and route geometry. Paths: `src/app/(app)/dean/dashboard/**`, `src/app/(app)/dean/college-oversight/**`, feature-local skeletons/tests. Acceptance: headings and period controls remain available while delayed matrices/totals resolve; completed and active academic-period states, risk filters, program filters, and empty states preserve their present behavior; only intended local regions suspend. Verify: focused route tests, production-authenticated Fast 3G/4x CPU LCP traces, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(dean): stream oversight read regions`.
- [ ] 5.3 Replace Dean Dashboard total computation from full readiness detail with a purpose-built aggregate or bounded projection, proving result parity for active and completed academic periods. Scope: Dean readiness totals only. Paths: `src/features/dean/services/read-dean-oversight.ts`, `src/features/academic-calendar/services/read-period-readiness.ts` only if required, focused tests. Acceptance: displayed active, ready, missing-CILO, and incomplete-mapping totals match the current model; detail routes retain their required context data; no academic-period authorization changes. Verify: focused unit tests and disposable-DB parity test where applicable, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(dean): aggregate dashboard readiness totals`.

## 6. Scoped Freshness and Invalidation

- [ ] 6.1 Inventory catalog and academic-period read models, classify each against the design freshness matrix, and create a minimal centralized ownership module for declared tags/route sets without enabling Next.js Cache Components. Scope: cache contract and helpers only. Paths: new or existing `src/lib/cache/**`, feature read services, action tests. Acceptance: every candidate declares exact cache API, key tuple/serialization, complete projection, freshness/stale policy, deployment-cache behavior, mutation-to-tag matrix, invalidation owner, and authorization boundary; entries with unknown values are ineligible; sessions, profiles, rosters, assignments, responses, and comments are explicitly excluded. Verify: codebase-design seam review, focused helper tests and cache-matrix review, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(cache): define scoped read freshness ownership`.
- [ ] 6.2 Convert one measured low-volatility catalog or academic-period projection to the approved exact server-cache mechanism and add complete invalidation for its owning writes while retaining route invalidation during transition. Scope: one domain selected from programs, courses, periods, or instrument metadata after baseline review. Paths: owning feature service, owning Server Actions, `src/lib/cache/**`, action/service tests. Acceptance: cache functions accept only declared primitive non-user dimensions; an unauthorized request cannot invoke the persistent-cache function; distinct declared key tuples do not share results; every owning mutation invalidates the declared tag set; Cache Components remain disabled. Verify: focused key-isolation and invalidation tests, representative browser refresh, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `perf(cache): cache <selected-shared-read-model>`.
- [ ] 6.3 Repair audited incomplete route invalidation in the converted domain and add tests asserting complete affected route/tag sets, including Dean projections where shared data changes. Scope: cache/invalidation correctness. Paths: relevant `src/lib/actions/*.ts`, domain tests. Acceptance: actions no longer leave a declared shared projection stale; no unrelated private route is broadly invalidated solely as a substitute for precise ownership. Verify: focused Server Action tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(cache): complete <selected-domain> invalidation`.

## 7. Final Regression and Performance Verification

- [ ] 7.1 Run cross-slice Vitest regression and documented Chrome DevTools browser verification for role/account-state gates, nested active navigation, list URL state, Server Action mutations, private-data cache exclusion, and deferred visualization fallbacks. Scope: automated and browser evidence for all delivered slices. Paths: `src/__tests__/app/**`, `src/__tests__/components/**`, `src/__tests__/features/**`, `src/__tests__/lib/actions/**`. Acceptance: each new OpenSpec scenario has focused automated evidence or a documented browser evidence check; no cross-role data is rendered or cached; no scenario is called end-to-end unless a browser runner executes it in CI. Verify: `pnpm test`, relevant focused Vitest commands, disposable DB tests only when opted in, a11y-debugging, Chrome DevTools, `pnpm lint`, `pnpm build`. Commit: `test(perf): cover role rendering and read model regressions`.
- [ ] 7.2 Compare final production-build browser traces against the baseline for Secretary Course Assignments, Dean Dashboard, and Faculty Dashboard, then record residual limitations and follow-up candidates without expanding scope. Scope: verification evidence and follow-up documentation only. Paths: change evidence or existing developer documentation if needed. Acceptance: evidence records the production authentication setup, viewport, Fast 3G/4x CPU throttle, selected LCP element, LCP breakdown, document/fetch/script waterfall, and confirms server-first initial Course Assignments data, no internal Dean HTTP serialization for Server Component reads, and deferred Faculty visualization chunks; unresolved Cache Components or offline work remains explicitly deferred. Verify: `pnpm build`, Chrome DevTools performance and network traces, LCP insight analysis, Lighthouse accessibility snapshots, `pnpm lint`, `pnpm test`. Commit: `docs(perf): record navigation rendering verification`.
