## 1. Dual-View Course Roster Discovery

- [ ] 1.1 Deliver complete List and Card discovery end to end with accessible URL-backed view selection. Scope: add `src/components/ui/toggle-group.tsx` through `pnpm dlx shadcn@latest add toggle-group`, review the generated Base UI source, add `src/features/course-assignments/components/course-roster-view-selector.tsx`, and modify `src/app/(app)/faculty/course-rosters/page.tsx`, `src/features/course-assignments/components/course-roster-pages.tsx`, `src/__tests__/app/course-roster-routes.test.tsx`, `src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, and `src/features/design-system/data/production-surface-inventory.ts`. Acceptance: List is canonical default; selecting either List or Card renders a complete usable presentation; both show Course, Program, class context, Academic Period, lifecycle state, distinct active-roster and current evaluation-eligible counts, and one open action; desktop List uses existing shadcn Table semantics, mobile List uses compact stacked rows without page-level horizontal scrolling, and Card adapts from one to two to three columns without nested metric cards; selector cannot enter no-selection state, selected state is visible and announced, switching preserves search/history and resets page, Card survives search/history/pagination/refresh/canonical redirects, invalid view values retain safe not-found handling, repeated current-Faculty identity is omitted, every new production surface has one inventory disposition, and no client fetch or authorization logic exists. Verify: `pnpm vitest run src/__tests__/app/course-roster-routes.test.tsx src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm lint`, `pnpm build`, Fallow changed-code boundary/dead-code audit, and browser checks at 375, 768, 1024, and 1440 px in light/dark with keyboard and coarse-pointer emulation. Commit: `feat(course-rosters): add dual-view roster discovery`.

## 2. Operational States And Design-System Parity

- [ ] 2.1 Deliver the redesigned page hierarchy, compact filters, recoverable safe states, and preserved server-owned invariants. Scope: add `src/features/course-assignments/components/course-roster-retry.tsx`, modify `src/features/course-assignments/components/course-roster-pages.tsx`, modify `src/app/(app)/faculty/course-rosters/loading.tsx` only if the existing generic skeleton does not match, update `src/features/design-system/data/production-surface-inventory.ts`, and add relevant route/component tests. Acceptance: static role context does not resemble a link; filters use project field/input/checkbox/button patterns; results toolbar combines heading, count/history state, and selector; installed `Empty` distinguishes filtered-empty from no-current-assignment state and provides clear-filters or include-history recovery where applicable; service-returned safe errors retain opaque references and expose a current-URL retry leaf without diagnostics; valid view state does not bypass inactive-account behavior; List and Card remain server-authorized and request-scoped; no new client data fetch, shared cache, Cache Components, PPR, manual prefetch, dependency, schema, authorization, or privacy behavior exists; loading remains stable and meaningful; semantic tokens, focus, pressed, coarse-pointer, dark appearance, and reduced-motion behavior match `docs/design.md`; issue #143 remains owner for full workflow acceptance. Verify: `pnpm vitest run src/__tests__/app/course-roster-routes.test.tsx src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm test`, `pnpm lint`, `pnpm build`, Fallow changed-code audit, and browser verification of retry/loading/empty states. Commit: `style(course-rosters): align roster discovery workspace`.
