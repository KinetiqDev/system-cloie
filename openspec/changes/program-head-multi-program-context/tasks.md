## Required Implementation And Verification Skills

- `codebase-design` is required for slice 1.1 to review the `resolve-program-head-context` module interface, its transaction revalidation seam, and its test surface before feature callers migrate.
- `domain-modeling` is required for slices 1.1 and 7.1 to update the Identity and Access glossary and record ADR `0009-program-head-selected-program-context.md` without adding implementation detail to `CONTEXT.md`.
- `next-best-practices` is required for slices 1.2 through 6.2 for dynamic App Router segments, async `params`, Server Components, redirects, Server Actions, loading/error boundaries, and dynamic-path revalidation.
- `shadcn` is required for slices 1.2 and 7.1 before composing the Program selector, no-assignment state, selected-Program header, and Secretary checkbox assignment-set fieldset. Use installed Base UI primitives and do not add Radix packages.
- `supabase` and `supabase-postgres-best-practices` are required for slice 1.1 and before any later proposed schema change. This change is expected to make no remote writes, Prisma model edits, SQL migrations, or generated-type edits; inspect the query/index evidence again if implementation proposes an exception.
- `a11y-debugging` is required for slices 1.2 and 7.1 to validate selector and assignment-set keyboard behavior, labels, focus order, and error announcements.
- `chrome-devtools` is required for slice 8.1 browser verification after the isolated dedicated-demo auth boundary checks. It is not used against primary Production or to write the linked Supabase project.

## 1. Program Context Foundation And Dashboard

- [x] 1.1 Establish the Program Head authority seam, glossary, ADR, route helpers, and multi-Program fixtures. Scope: add a deep server-only resolver that returns the complete active assignment set and a selected Program only after request-time validation; add a transaction-safe active-assignment recheck; centralize Program Head route construction; document that assignment-set authority is distinct from account role and that primary/default Program semantics are rejected. Paths: `src/features/auth/services/resolve-program-head-context.ts`, `src/lib/constants/program-head-routes.ts`, `src/features/auth/CONTEXT.md`, `docs/adr/0009-program-head-selected-program-context.md`, `prisma/seed/fixtures/users.ts`, related seed constants/seed tests, `src/__tests__/features/auth/resolve-program-head-context.test.ts`, and `src/__tests__/lib/program-head-routes.test.ts`. Acceptance: a `PROGRAM_HEAD` User with BEED and BSED resolves both authorized Programs and only a requested assigned Program as selected; zero active assignments, wrong role, malformed ID, inactive assignment, and unassigned Program return safe failures; the transaction helper rejects a revoked selected assignment; fixtures include one multi-Program Program Head without adding a second role; no `primary_program_id`, persistent authorization cache, Prisma model change, SQL migration, or generated type edit is introduced. Verify: focused resolver/seed/route tests; `pnpm lint`; `pnpm test`; `pnpm build`. Commit: `feat(auth): add program head context resolver`.

- [x] 1.2 Deliver Program Head entry selection and selected Program dashboard. Scope: add `/program-head` entry state for zero, one, and multiple active assignments; add `/program-head/programs/[programId]` dynamic layout with a selected Program context header and accessible selector return link; move the Dashboard into the canonical selected Program route; replace static Program Head Dashboard with a compatibility redirect; make dashboard reads use the selected Program service entry rather than `findFirst`; retain existing loading/error geometry at the new route. Paths: `src/app/(app)/program-head/page.tsx`, `src/app/(app)/program-head/programs/[programId]/layout.tsx`, `src/app/(app)/program-head/programs/[programId]/dashboard/page.tsx`, `src/app/(app)/program-head/programs/[programId]/dashboard/loading.tsx`, `src/app/(app)/program-head/programs/[programId]/error.tsx`, `src/app/(app)/program-head/dashboard/page.tsx`, `src/app/(app)/program-head/error.tsx`, `src/features/analytics/services/get-program-head-dashboard.ts`, new Program selector/context-header component under `src/features/auth/components/` or `src/features/analytics/components/`, and route/component tests. Acceptance: zero assignments show an actionable empty state; one assignment redirects directly to that Program dashboard; multiple assignments render a deliberate selector without management data; a BEED dashboard contains only BEED data; an unassigned or invalid dynamic segment is denied without leaking Program data; old `/program-head/dashboard` opens `/program-head` and never chooses an assignment by order; selector and context header are keyboard-accessible and use installed shadcn/Base UI `Empty`, `Card`, and `Button` composition. Verify: focused Program Head route/dashboard tests, `a11y-debugging` selector keyboard/focus checks, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(program-head): add selected program dashboard`.

- [x] 1.3 Make Program Head global navigation preserve or deliberately acquire selected Program context. Scope: derive Program Head management navigation links from the current dynamic pathname through centralized path builders; retain `/program-head/profile` as account-scoped; ensure legacy/static and profile navigation return to `/program-head` instead of guessing a Program; migrate Program Head loading/error recovery return links that only concern the foundation route family. Paths: `src/lib/constants/navigation.ts`, `src/components/layout/app-shell.tsx`, `src/components/layout/sidebar.tsx`, `src/components/layout/mobile-nav.tsx`, `src/components/layout/mobile-sidebar-drawer.tsx`, `src/components/layout/topbar.tsx` only if Program context visibility belongs there, `src/app/(app)/program-head/profile/page.tsx`, `src/__tests__/lib/navigation.test.ts`, and layout/navigation component tests. Acceptance: when a Program Head is inside BSED canonical routes, desktop and mobile navigation preserves BSED; copied invalid Program URLs remain server-denied; Program Head Profile shows the complete assignment set but does not establish selected authority; non-Program Head and Dean navigation behavior remains unchanged. Verify: focused navigation/layout/profile tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(navigation): preserve program head route context`.

## 2. Learning Outcomes Per Selected Program

- [x] 2.1 Deliver selected-Program Graduate Outcomes and CILO mapping review. Scope: migrate Outcomes and Mapping canonical pages, services, schemas/actions, component links, confirmation write inputs, and exact selected-path revalidation from implicit first-assignment selection to explicit Program context. Paths: `src/app/(app)/program-head/programs/[programId]/outcomes/page.tsx`, `src/app/(app)/program-head/programs/[programId]/outcomes/mapping/page.tsx`, static legacy Outcomes and Mapping route redirects, `src/features/outcomes/services/manage-program-head-outcomes.ts`, `src/features/outcomes/services/manage-outcome-writes.ts`, `src/features/outcomes/components/program-head-outcomes-page.tsx`, `src/features/outcomes/components/go-form-dialog.tsx`, `src/lib/actions/program-head-outcome-actions.ts`, `src/features/outcomes/schemas/go.ts`, and outcome/action/route tests. Acceptance: selected BSED lists, creates, reorders, updates, archives, and maps only BSED Outcomes/Courses; an actor managing BEED and BSED cannot target a BEED Outcome from a BSED selected route; the serializable write transaction revalidates selected assignment and target ownership; static routes redirect to entry; successful writes revalidate only the selected canonical Outcomes/Mapping routes plus any required existing role route; no `programIds[0]` remains. Verify: `pnpm vitest run src/__tests__/modules/outcomes`, focused action/route tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(outcomes): scope program head outcomes by route`.

## 3. Program-Specific Course And Assignment Management

- [x] 3.1 Deliver selected-Program Program-specific Course catalog management. Scope: migrate the Courses page, course resolver, create/edit/toggle service and action inputs, form data, links, and revalidation to selected Program context. Paths: `src/app/(app)/program-head/programs/[programId]/courses/page.tsx`, static `src/app/(app)/program-head/courses/page.tsx`, `src/features/academic-structure/services/resolve-program-head-courses.ts`, `src/features/academic-structure/services/manage-program-head-courses.ts`, `src/features/academic-structure/schemas/program-head-course.ts`, `src/features/academic-structure/components/program-head-courses-catalog.tsx`, `src/lib/actions/program-head-course-actions.ts`, and academic-catalog/action/route tests. Acceptance: selected BSED lists only BSED Program-specific Courses and the applicable read-only General Education view; Program-wide Course creation uses BSED explicitly, not the first assignment; a selected major must belong to BSED; edit/toggle commands reject BEED records from BSED context and revalidate scope inside the write; General Education management stays forbidden. Verify: `pnpm vitest run src/__tests__/modules/academic-catalog-and-context/manage-program-head-courses.test.ts`, focused route/action tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(courses): scope program head catalog by program`.

- [ ] 3.2 Deliver selected-Program Course Assignment management. Scope: migrate Course Assignments list state, Server Component page, form options, service query inputs/policies, mutation action scope, selected Program route revalidation, and legacy redirect. Paths: `src/app/(app)/program-head/programs/[programId]/course-assignments/page.tsx`, static `src/app/(app)/program-head/course-assignments/page.tsx`, `src/features/course-assignments/course-assignment-list-state.ts`, `src/features/course-assignments/services/load-course-assignment-list-page.ts`, `src/features/course-assignments/services/list-course-assignments.ts`, `src/features/course-assignments/services/manage-course-assignments.ts`, `src/features/course-assignments/components/course-assignments-page-shell.tsx`, `src/features/course-assignments/components/course-assignment-form-dialog.tsx`, `src/lib/actions/course-assignment-actions.ts`, course assignment schemas/types, and list/manage/action/route tests. Acceptance: selected BSED returns and manages only BSED Course Assignments; Program Head cannot widen the list through a query parameter or submit a BEED assignment through BSED context; Course/Program ownership and General Education restrictions remain enforced; destructive and lifecycle writes revalidate active BSED assignment inside the existing transaction; successful writes refresh exact BSED routes while retaining Secretary/Dean/Faculty route freshness. Verify: focused `load-course-assignment-list-page`, list/manage/action/route suites, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(course-assignments): scope program head assignments by program`.

- [ ] 3.3 Deliver selected-Program roster access and roster mutation navigation. Scope: add the Program Head-specific selected-Program roster wrapper; carry selected Program through list/detail/back links, roster actions, imports, and revalidation; preserve generic roster URLs for Faculty, Secretary, and Dean. Paths: `src/app/(app)/program-head/programs/[programId]/course-rosters/[assignmentId]/page.tsx`, `src/app/(app)/course-rosters/[assignmentId]/page.tsx`, `src/features/course-assignments/services/read-course-rosters.ts`, `src/features/course-assignments/services/manage-course-roster.ts`, `src/features/course-assignments/services/import-course-roster.ts`, `src/features/course-assignments/components/course-roster-pages.tsx`, `src/lib/actions/course-roster-actions.ts`, related roster schemas/types, and roster route/action tests. Acceptance: a Program Head opening a BSED selected roster can view or mutate only a BSED authorized Course Assignment; a BEED assignment from BSED context is non-disclosing unavailable; roster write transactions revalidate active selected assignment and existing roster lifecycle constraints; links return to selected BSED Course Assignments; Faculty, Secretary, and Dean generic roster behavior is unchanged. Verify: focused roster read/manage/import/action/route tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(rosters): retain program head selected context`.

## 4. Program Instruments And Central Deployments

- [ ] 4.1 Deliver selected-Program instrument template and baseline-copy management. Scope: migrate Tools, new/edit template pages, template/baseline services, actions, builder redirects, links, and path revalidation to selected Program context. Paths: `src/app/(app)/program-head/programs/[programId]/tools/page.tsx`, `tools/new/page.tsx`, `tools/[id]/edit/page.tsx`, static legacy Tools pages, `src/features/instruments/services/manage-program-head-templates.ts`, `src/features/instruments/services/create-baseline-copy.ts`, `src/features/instruments/components/program-head-tools-page.tsx`, `src/features/instruments/components/program-head-template-builder.tsx`, `src/features/instruments/components/template-builder.tsx`, `src/lib/actions/program-head-template-actions.ts`, template schemas, and instrument/action/route tests. Acceptance: selected BSED shows and creates only BSED-owned templates; a BSED route cannot edit, activate, delete, or duplicate a BEED-owned template even when the actor also manages BEED; an institutional baseline copy is created only for BSED; template lifecycle/version rules stay intact; successful mutations revalidate selected BSED Tools pages and do not use static paths or first Program selection. Verify: `pnpm vitest run src/__tests__/modules/instruments/manage-program-head-templates.test.ts`, focused action/route/component tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(instruments): scope program head tools by program`.

- [ ] 4.2 Deliver selected-Program central deployment publishing, respondent preview, list, and closing. Scope: migrate selected Program through publish page/form, Zod schema/type input, preview service, publish/close transaction, deployment list, redirect, and revalidation; eliminate obsolete deployment invalidation. Paths: `src/app/(app)/program-head/programs/[programId]/tools/publish/page.tsx`, `src/features/evaluations/components/publish-central-deployment-form.tsx`, `src/features/evaluations/schemas/central-deployment.ts`, `src/features/evaluations/types.ts`, `src/features/evaluations/services/preview-central-deployment-respondents.ts`, `src/features/evaluations/services/publish-central-deployment.ts`, `src/features/evaluations/services/list-program-head-deployments.ts`, `src/lib/actions/central-deployment-actions.ts`, and deployment/action/route tests. Acceptance: selected BSED deployment list and label include only BSED records; publish accepts BSED only after active-assignment, template, major, and respondent eligibility checks; client-curated respondent IDs cannot introduce another Program's Users; close rejects a BEED deployment from BSED context; the transaction revalidates assignment/resource ownership before creating or closing; actions revalidate exact selected BSED Tools paths and remove the nonexistent static deployment path. Verify: `pnpm vitest run src/__tests__/modules/deployments-and-targeting/publish-central-deployment.test.ts`, focused preview/list/action/route tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(deployments): scope program head publishing by program`.

## 5. Course-Bound Evaluation And Review Context

- [ ] 5.1 Deliver selected-Program on-behalf Course-bound evaluation publication. Scope: migrate selected Program through new evaluation route, template/assignment context resolution, course-bound respondent preview, publish Server Action payload, success redirect, and route revalidation while preserving non-Program Head role flows. Paths: `src/app/(app)/program-head/programs/[programId]/cilo-evaluations/new/page.tsx`, `src/features/evaluations/services/publish-course-bound-evaluation.ts`, `src/features/evaluations/services/preview-course-bound-respondents.ts`, `src/features/evaluations/components/publish-course-bound-evaluation-form-v2.tsx`, `src/lib/actions/course-bound-evaluation-actions.ts`, evaluation schemas/types, and evaluation route/action tests. Acceptance: selected BSED exposes only eligible BSED Program-specific Course Assignments for on-behalf publication; a BEED assignment/template cannot be published through BSED context; the existing publish transaction revalidates selected assignment/resource authority; success returns to selected BSED Tools; Faculty, Secretary, and Dean actions retain their existing scope rules. Verify: focused Course-bound publish/preview/action/route tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(evaluations): scope program head publication by program`.

- [ ] 5.2 Deliver selected-Program Course-bound reviews and response review navigation. Scope: migrate Program Head review list/detail/response routes and their reader service calls, links, selected Program mismatch handling, back paths, loading/error files, and relevant review mutations/late inclusion revalidation. Paths: `src/app/(app)/program-head/programs/[programId]/cilo-reviews/page.tsx`, `[evaluationId]/page.tsx`, `[evaluationId]/responses/[responseId]/page.tsx`, static review routes, `src/features/analytics/services/resolve-reviewer-program-scope.ts`, `src/features/analytics/services/list-course-bound-review-items.ts`, `src/features/analytics/services/get-course-bound-review-detail.ts`, `src/features/analytics/services/get-course-bound-response-review.ts`, `src/features/evaluations/services/late-include-course-bound-evaluation.ts`, `src/lib/actions/course-bound-evaluation-actions.ts`, and reviewer/action/route tests. Acceptance: BSED selected review lists and details exclude BEED evaluations even for a multi-Program Program Head; a direct BEED evaluation URL under BSED returns the existing non-disclosing unavailable result; evaluation/response links and back links preserve BSED; Course-bound changes refresh selected BSED review paths without weakening Faculty/Secretary/Dean behavior. Verify: `pnpm vitest run src/__tests__/modules/analytics-reporting-and-review`, focused reviewer/action/route tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(reviews): scope program head reviews by program`.

## 6. Selected-Program Insights And Reporting

- [ ] 6.1 Deliver selected-Program analytics and reports route context. Scope: move the current Program Head Analytics and Reports routes into the canonical dynamic family, render selected Program identity and selected-only authorized data or current placeholder state, migrate static URLs to entry redirects, and preserve a clear separation from future cross-Program aggregation. Paths: `src/app/(app)/program-head/programs/[programId]/analytics/page.tsx`, `src/app/(app)/program-head/programs/[programId]/reports/page.tsx`, static `src/app/(app)/program-head/analytics/page.tsx`, static `src/app/(app)/program-head/reports/page.tsx`, `src/features/analytics/services/get-program-head-dashboard.ts` and any current Program Head reporting service, route helpers, and analytics/report route tests. Acceptance: selected BSED Analytics/Reports never show an implicit combined BEED+BSED total; pages label BSED or an explicitly scoped placeholder; static URLs do not infer a Program; any future cross-Program summary remains unavailable from these management routes. Verify: focused analytics/report route/service tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(analytics): scope program head insights by program`.

## 7. Secretary Assignment-Set Administration

- [ ] 7.1 Deliver protected Secretary administration of the complete Program Head assignment set. Scope: change Program Head edit schema, action form parsing, edit projection, confirmation payload/review, assignment-set transaction, and Secretary dialog from singular Program selection to an accessible Program checkbox fieldset; reuse/consolidate existing assignment lifecycle upsert/deactivation behavior; retain exactly one initial Program for Secretary-created Program Head accounts. Paths: `src/features/users/schemas/edit-user.ts`, `src/lib/actions/secretary-edit-user-actions.ts`, `src/features/users/services/get-user-edit-record.ts`, `src/features/users/services/edit-user-by-secretary.ts`, `src/features/users/services/manage-users.ts`, `src/features/users/components/secretary-users-list/edit-user-dialog.tsx`, `src/features/users/services/create-user-by-secretary.ts`, `src/__tests__/features/users/services/edit-user-by-secretary.test.ts`, Secretary action/component tests, and `src/features/auth/CONTEXT.md` only for resolved glossary wording. Acceptance: a Secretary can add, retain, remove, and restore BEED/BSED assignments as one protected assignment-set save; the review names the exact before/after sets; selected historical rows reactivate, unknown rows create once, unselected active rows deactivate, and no row is deleted/recreated solely to change selection; self-edit, non-Secretary, wrong-role, inactive Program, stale confirmation, and duplicate race conditions are safely rejected; role revocation still requires zero active assignments; the checkbox fieldset is labeled, keyboard-operable, and uses Base UI/shadcn form composition; no multi-role behavior or primary Program is introduced. Verify: focused user service/action/component tests; `a11y-debugging` keyboard and accessible-name checks; `pnpm lint`; `pnpm test`; `pnpm build`. Commit: `feat(users): manage program head assignment sets`.

## 8. Cross-Program Regression And Release Evidence

- [ ] 8.1 Remove every remaining implicit Program Head selection and prove selected-context isolation across the application. Scope: repeat the repository inventory for `programIds[0]`, `program_head_assignments[0]`, Program Head `findFirst`, singular assignment fields, static Program Head route strings, missing Program cache/revalidation dimensions, and Program-scoped client payloads; fix only residual Program Head context defects found by that inventory; update affected tests and change evidence. Paths: affected Program Head files discovered by the final inventory, `src/__tests__/app/**`, `src/__tests__/features/**`, `src/__tests__/modules/**`, `src/__tests__/lib/actions/**`, and this change's artifacts/evidence. Acceptance: no Program Head read or command derives a Program from unordered data; selected Program/resource mismatches are denied for dashboard, outcomes, Courses, assignments, rosters, templates, deployments, reviews, analytics, and reports; zero/one/multiple/inactive/unauthorized/stale conditions have automated coverage; no Program Head private read is persistently cached; the linked Supabase project remains unchanged; remote RLS/grant findings are recorded as an out-of-scope security follow-up rather than declared fixed. Verify: `rg "programIds\\[0\\]|program_head_assignments\\[0\\]|programHeadAssignment\\.findFirst|findFirst\\(" src/app src/features src/lib/actions`; focused suites from slices 1-7; `pnpm lint`; `pnpm test`; `pnpm build`; isolated demo browser trace only after `pnpm verify:dedicated-demo-auth-boundary` and `pnpm verify:demo-target-isolation`. Commit: `test(program-head): cover multi-program context boundaries`.
