## 1. Canonical Name Schema And Migration Foundation

- [x] 1.1 Deliver the expandable canonical `User.name` database contract. Scope: update `prisma/models/identity-access.prisma`, add the repository-generated Supabase migration for `users.name`, inventory and validate existing `users.first_name`/`users.last_name` rows, backfill with trimmed `concat_ws` semantics, reject blank results without placeholders, make legacy split columns nullable during the compatibility window, and install a temporary database trigger that derives `name` for old split-field writers while preserving explicit name-only writes. Paths: `prisma/models/identity-access.prisma`, `supabase/migrations/<generated-canonical-user-name-expansion-migration>.sql`, `src/__tests__/prisma/` or the affected migration/data-invariant test path. Acceptance: existing two-part, single-part, whitespace, and invalid legacy rows have explicit outcomes; valid rows retain their visible combined name; old application-style writes continue through the bridge; new name-only writes are preserved; the migration is safe to dry-run and does not alter roles, account states, profile records, enrollments, rosters, evaluations, or foreign keys. Verify: migration SQL review, `pnpm supabase:push:dry-run`, focused migration/data tests, `pnpm exec prisma validate --schema prisma`. Commit: `refactor(identity): add canonical user name column`.
- [ ] 1.2 Regenerate schema clients and establish the application-facing `User.name` type seam. Scope: regenerate Prisma Client and `src/types/supabase-database.ts` after the expansion migration; add focused generated-schema assertions without hand-editing generated output. Paths: `src/types/supabase-database.ts`, `src/__tests__/scripts/supabase-workflow-helpers.test.ts` or a new focused schema contract test. Acceptance: Prisma and generated Supabase types expose required `name`, no application type relies on removed fields as the canonical contract, and the generated file is produced by `pnpm supabase:types`. Verify: `pnpm supabase:types`, `pnpm exec prisma generate --schema prisma`, `pnpm exec prisma validate --schema prisma`, focused schema tests. Commit: `build(identity): regenerate canonical name types`.

## 2. Google-Authoritative OAuth Linking

- [x] 2.1 Deliver the server-only Google account-name resolver and callback first-link behavior. Scope: add `src/features/auth/services/resolve-google-account-name.ts`; modify `src/app/api/auth/callback/route.ts` so provider precedence is `name`, `full_name`, then `given_name + family_name`; new accounts use the resolved name; unlinked email matches atomically replace provisional name and set `auth_user_id`; linked accounts preserve stored name; missing first-link names fail safely; different linked Auth IDs fail closed; bootstrap Secretary uses provider identity without placeholders. Paths: `src/features/auth/services/resolve-google-account-name.ts`, `src/app/api/auth/callback/route.ts`, `src/app/(public)/status/[type]/page.tsx`, `src/__tests__/app/auth-callback-route.test.ts`, new resolver tests under `src/__tests__/features/auth/`. Acceptance: new, first-link, already-linked, Secretary-corrected, missing-name, identity-conflict, role-mismatch, inactive-account, bootstrap, and provider-precedence scenarios pass; no email-derived or invented names are persisted; existing legal-ticket, role, domain, account-state, and post-login behavior remains unchanged. Verify: focused resolver and callback tests, `pnpm vitest run src/__tests__/app/auth-callback-route.test.ts`, `pnpm lint`. Commit: `feat(auth): make google name authoritative at first link`.
- [x] 2.2 Deliver the explicit OAuth identity-name status and session regression contract. Scope: add the safe missing-name outcome to the status configuration and preserve existing linked-account login behavior when later metadata is absent; update callback/status tests and auth context documentation references where implementation copy is needed. Paths: `src/app/(public)/status/[type]/page.tsx`, `src/features/auth/CONTEXT.md`, `src/__tests__/app/auth-callback-route.test.ts`, relevant `src/__tests__/features/auth/` tests. Acceptance: a new or first-link account with no usable provider name receives actionable safe guidance and remains unlinked; an already-linked account can continue without provider name metadata; no internal identifiers or provider diagnostics are disclosed. Verify: focused status/callback tests, `pnpm lint`. Commit: `fix(auth): handle missing google account names safely`.

## 3. Self-Service Registration Without Editable Names

- [x] 3.1 Deliver the server-authoritative self-service registration contracts. Scope: remove name fields from Student, Faculty, Alumni, and Industry Partner schemas and Server Actions; resolve the authenticated domain User from the server session; update role-specific writes to preserve the OAuth-established `User.name` and reject or ignore client-injected name data. Paths: `src/lib/schemas/student-profile.ts`, `src/lib/schemas/faculty-profile.ts`, `src/lib/schemas/alumni-profile.ts`, `src/lib/schemas/industry-partner-profile.ts`, `src/lib/actions/onboarding-actions.ts`, `src/lib/actions/faculty-actions.ts`, `src/lib/actions/alumni-actions.ts`, `src/lib/actions/industry-partner-actions.ts`. Acceptance: each action still enforces authentication, role mismatch, program/major, verification, and deferred-enrollment rules; submitted FormData cannot replace the canonical name; Student active-term and deferred-enrollment behavior remains unchanged. Verify: focused `src/__tests__/features/auth/`, `src/__tests__/lib/actions/`, and schema tests; `pnpm lint`. Commit: `refactor(identity): remove client name from self-service actions`.
- [x] 3.2 Deliver the self-service onboarding UI contract. Scope: remove editable first/last inputs and fallback props from `src/app/(public)/onboarding/page.tsx`, `src/app/(public)/onboarding/student-profile-form.tsx`, and role onboarding forms; show a display-only explanation that Google supplies the account name where appropriate; retain existing academic and role-specific fields, validation, loading, error, cancel, and navigation behavior without adding a Client Component boundary. Paths: `src/app/(public)/onboarding/page.tsx`, `src/app/(public)/onboarding/student-profile-form.tsx`, `src/features/users/components/faculty-onboarding-form.tsx`, `src/features/users/components/alumni-onboarding-form.tsx`, `src/features/users/components/industry-partner-onboarding-form.tsx`, related component tests. Acceptance: no self-service form renders editable first/last/name controls; role-specific fields remain accessible and functional; the UI does not make client state authoritative for identity. Verify: focused onboarding schema/action/component tests, `pnpm lint`, `pnpm build`. Commit: `feat(onboarding): derive account name from google identity`.

## 4. Secretary Provisional Name And Correction Flow

- [x] 4.1 Deliver Secretary creation and edit with one provisional/canonical Name field. Scope: replace first/last inputs, FormData keys, Zod contracts, service writes, edit projections, protected dialog state, and user details rendering with one required `name`; preserve Secretary authorization, complete-at-creation role requirements, atomic role-specific writes, and protected academic/external confirmation behavior. Paths: `src/features/users/schemas/create-user.ts`, `src/features/users/schemas/edit-user.ts`, `src/features/users/schemas/update-user.ts`, `src/features/users/services/create-user-by-secretary.ts`, `src/features/users/services/edit-user-by-secretary.ts`, `src/features/users/services/get-user-edit-record.ts`, `src/features/users/services/update-user-by-secretary.ts`, `src/lib/actions/secretary-user-crud-actions.ts`, `src/lib/actions/secretary-edit-user-actions.ts`, `src/features/users/components/secretary-add-user-form.tsx`, `src/features/users/components/secretary-users-list/edit-user-dialog.tsx`, `src/features/users/components/secretary-users-list/user-dialogs.tsx`. Acceptance: all supported roles require one non-empty provisional name at Secretary creation; an authorized Secretary can correct a linked name; name correction does not trigger academic protected-change confirmation; role-specific completeness and account-state invariants remain unchanged. Verify: focused create/edit/get-record/action/component tests, `pnpm lint`, `pnpm build`. Commit: `refactor(users): replace split secretary names with canonical name`.
- [x] 4.2 Deliver the bounded Secretary Users list query and presentation contract. Scope: change summary DTOs, table/cards, filters, query parsing, default sorting, stable tie-breakers, and search to complete `name`; canonicalize legacy `sort=firstName` and `sort=lastName` values. Paths: `src/features/users/services/list-secretary-users-summary.ts`, `src/features/users/schemas/secretary-users-list.ts`, `src/features/users/components/secretary-users-list/index.tsx`, `src/features/users/components/secretary-users-list/users-filter-bar.tsx`, `src/features/users/components/secretary-users-list/users-data-table.tsx`, `src/__tests__/features/users/list-secretary-users-summary.test.ts`, `src/__tests__/features/users/secretary-users-list-query.test.ts`, `src/__tests__/components/secretary/secretary-users-list.test.tsx`. Acceptance: complete-name search and sort work server-side within the existing page bounds; legacy sort URLs do not execute surname semantics; list display and accessibility labels use one name; KPI, role, program, major, pagination, and authorization behavior remain unchanged. Verify: focused Users service/query/component tests, `pnpm lint`, `pnpm build`. Commit: `feat(users): search and sort secretary users by name`.

## 5. Downstream Canonical Name Projections

- [x] 5.1 Deliver canonical names through enrollment, academic-calendar, course-assignment, roster, and Dean read models. Scope: replace Prisma selections, DTO fields, concatenation, surname ordering, search predicates, audit actor labels, faculty selectors, roster members, and Dean roster display names with direct `name`; preserve server authorization, pagination, lifecycle, eligibility, and current-period behavior. Paths: `src/features/enrollments/types.ts`, `src/features/enrollments/services/list-enrollments.ts`, `src/features/enrollments/services/list-students-for-class.ts`, `src/features/academic-calendar/types.ts`, `src/features/academic-calendar/services/list-school-years.ts`, `src/features/academic-calendar/services/run-term-rollover.ts`, `src/features/course-assignments/types.ts`, `src/features/course-assignments/services/list-course-assignments.ts`, `src/features/course-assignments/services/load-all-program-course-assignments-page.ts`, `src/features/course-assignments/services/read-course-rosters.ts`, `src/features/course-assignments/services/search-faculty-pool.ts`, `src/features/course-assignments/components/course-assignment-form-dialog.tsx`, `src/features/course-assignments/components/course-assignments-page-shell.tsx`, `src/features/course-assignments/components/edit-course-assignment-dialog.tsx`, `src/features/course-assignments/components/shared/assignment-filters.tsx`, `src/features/course-assignments/components/shared/faculty-search-popover.tsx`, `src/features/dean/services/read-dean-oversight.ts`, affected module/component tests. Acceptance: no downstream DTO exposes firstName/lastName aliases; complete names render unchanged in enrollments, selectors, rosters, removal audit labels, and Dean oversight; existing role scope, selected Program context, roster eligibility, lifecycle locks, pagination, and privacy behavior remain unchanged. Verify: focused enrollment/course-assignment/roster/Dean tests, `pnpm lint`, `pnpm build`. Commit: `refactor(academic): project canonical user names`.
- [x] 5.2 Deliver canonical names through evaluation, response, profile, and dashboard surfaces. Scope: replace respondent DTOs, publish controls, accessibility labels, faculty/student names, dashboard greetings, profile pages, and response list projections with direct `name`; preserve evaluation targeting, anonymization, authorization, and existing account-state behavior. Paths: `src/features/evaluations/types.ts`, `src/features/evaluations/services/get-faculty-evaluation-detail.ts`, `src/features/evaluations/services/preview-central-deployment-respondents.ts`, `src/features/evaluations/services/preview-course-bound-respondents.ts`, `src/features/evaluations/components/publish-central-deployment-form.tsx`, `src/features/evaluations/components/publish-course-bound-evaluation-form-v2.tsx`, `src/features/responses/services/list-student-assigned-evaluations.ts`, `src/features/responses/services/list-student-course-bound-evaluations.ts`, role profile/dashboard pages under `src/app/(app)/`, affected evaluation/response/app/component tests. Acceptance: respondent previews and publish controls never format names as `last, first`; profiles and dashboards show canonical names; anonymized response surfaces remain anonymized; evaluation recipient identity, assignment, and privacy invariants are unchanged. Verify: focused evaluation/response/profile/dashboard tests, `pnpm lint`, `pnpm build`. Commit: `refactor(evaluations): use canonical user names in projections`.

## 6. Seed, Demo, And Documentation Reconciliation

- [x] 6.1 Deliver deterministic canonical-name seed and bootstrap behavior. Scope: convert seed fixtures, seed runner writes, outline-defense bootstrap inputs and safety comparison, and related tests to one `name`; retain development and dedicated-demo fixture authority without invoking Google resolution. Paths: `prisma/seed/fixtures/users.ts`, `prisma/seed/runners/seed-users.ts`, `scripts/bootstrap-outline-defense-demo.ts`, `src/lib/constants/demo-users.ts` only if required by the renamed contract, `src/__tests__/scripts/bootstrap-outline-defense-demo.test.ts`, seed tests, demo-auth tests. Acceptance: repeated `pnpm db:seed` remains deterministic; the outline bootstrap refuses mismatched canonical markers and safely reuses matching users; development and dedicated-demo sessions use seeded names and preserve normal authorization/account-state checks. Verify: focused seed/bootstrap/demo tests, `pnpm db:seed` only against the configured disposable development target, `pnpm lint`. Commit: `refactor(seed): migrate fixtures to canonical user names`.
- [x] 6.2 Verify and reconcile durable identity documentation. Scope: confirm implementation terminology and behavior match `docs/adr/0014-google-authoritative-account-names.md`, the superseded name portions of `docs/adr/0001-complete-secretary-created-accounts.md`, `docs/adr/0002-separate-domain-users-from-auth-identities.md`, `src/features/auth/CONTEXT.md`, and `docs/agents/discrepancies-prd-srs-vs-current.md`; update only if implementation reveals a documented mismatch. Paths: `docs/adr/0014-google-authoritative-account-names.md`, `docs/adr/0001-complete-secretary-created-accounts.md`, `docs/adr/0002-separate-domain-users-from-auth-identities.md`, `src/features/auth/CONTEXT.md`, `docs/agents/discrepancies-prd-srs-vs-current.md`. Acceptance: no authoritative document requires first/last-name semantics or contradicts first-link Google authority; demo/development exceptions, Secretary provisional names, later-login preservation, and Secretary correction are stated consistently. Verify: `rg` documentation audit, `openspec validate --changes`, focused documentation/contract tests if applicable. Commit: `docs(identity): reconcile canonical name authority`.

## 7. Contract Cleanup And Release Verification

- [ ] 7.1 Complete the database contract transition after all consumers migrate. Scope: verify no application, test, seed, generated-type, migration, or deployment artifact requires `first_name`/`last_name`; apply the reviewed contract migration to remove the temporary compatibility trigger and drop obsolete columns; regenerate types and record migration status. Paths: `supabase/migrations/<generated-canonical-user-name-contract-migration>.sql`, `prisma/models/identity-access.prisma`, `src/types/supabase-database.ts`, migration workflow tests and evidence artifacts. Acceptance: PostgreSQL and Prisma expose required `users.name` only; no stale split-field references remain outside explicitly historical documentation/tests that are intentionally updated; the compatibility bridge is removed only after the name-based release is verified; rollback limitations are recorded and no automatic name splitting is claimed. Verify: `rg` repository inventory, `pnpm supabase:push:dry-run`, `pnpm supabase:types`, `pnpm exec prisma validate --schema prisma`, focused migration tests. Commit: `refactor(identity): remove legacy split name columns`.
- [ ] 7.2 Complete change-level regression and release evidence. Scope: run the full affected test inventory, lint, production build, and any approved browser/OAuth verification; confirm existing authorization, account-state, enrollment, roster, evaluation, privacy, cache, and demo boundaries. Paths: affected `src/__tests__/**`, `pnpm` verification outputs, OpenSpec change evidence if created. Acceptance: focused first-link and canonical projection scenarios pass; `pnpm test`, `pnpm lint`, and `pnpm build` pass; database integration remains gated; no shared production database is mutated; browser evidence distinguishes real OAuth from development/demo auth if performed. Verify: `pnpm test`, `pnpm lint`, `pnpm build`, focused Vitest commands, and approved `pnpm playwright test` or browser trace only when prerequisites pass. Commit: `test(identity): verify canonical name migration`.
