## 1. Student ID compatibility cutover

- [x] 1.1 Deliver Student onboarding and profile behavior without Student ID while retaining the nullable database column. Scope: remove Student ID from `src/lib/schemas/student-profile.ts`, `src/lib/actions/onboarding-actions.ts`, `src/app/(public)/onboarding/student-profile-form.tsx`, `src/app/(app)/student/profile/page.tsx`, and their focused tests; preserve canonical-name authority, active-term enrollment, deferred enrollment, Program, conditional Major, year, and section behavior. Acceptance: active and deferred Student onboarding complete without accepting/writing Student ID and profile UI omits it. Verify: `pnpm vitest run src/__tests__/schemas/student-profile.test.ts` plus affected onboarding/profile tests, `pnpm lint`, `pnpm build`. Commit: `feat(identity): remove student id from onboarding and profile`.

- [x] 1.2 Deliver Secretary Student creation and protected editing without Student ID while retaining the compatibility column. Scope: update `src/features/users/schemas/`, Secretary forms/actions/services/read models, and focused tests; preserve atomic complete account creation, conditional Major, active-term `SECRETARY` enrollment, deferred enrollment, protected review tokens, and canonical name behavior. Acceptance: Secretary create/edit neither renders, validates, sends, reads, nor writes Student ID. Verify: focused Secretary user schema/service/component/action tests, `pnpm lint`, `pnpm build`. Commit: `feat(users): remove student id from student management`.

- [x] 1.3 Deliver roster and evaluation eligibility without the Student ID completeness proxy. Scope: update `src/features/course-assignments/services/course-assignment-roster.ts`, roster read/import/mutation selections and types, enrollment class projections, Course-bound and central respondent preview DTOs/services/components, response eligibility fixtures, and focused tests; enforce valid active Program and conditional active Program-owned Major plus separate active placement. Acceptance: absent former ID never causes `PROFILE_INCOMPLETE`; invalid profile affiliation still does; Student ID appears in no production DTO/UI. Verify: focused roster eligibility, enrollment, evaluation preview/publication, and response workflow tests, `pnpm lint`, `pnpm build`. Commit: `refactor(students): replace student id completeness semantics`.

- [x] 1.4 Reconcile seed/demo data and active contracts for the Student ID compatibility release. Scope: remove Student ID fixture values from `prisma/seed/`, `scripts/bootstrap-outline-defense-demo.ts`, remaining tests and active docs; preserve deterministic users, explicit memberships, enrollments, evaluation assignments, and demo isolation. Acceptance: repository search finds the former field only in historical migrations, compatibility Prisma/generated types, and explicit migration-planning evidence. Verify: focused seed/config tests, `pnpm db:seed` against a disposable environment when available, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `chore(students): remove student id application dependencies`.

## 2. Name roster parsing and authorized preview

- [x] 2.1 Deliver the bounded name CSV contract end to end. Scope: replace the email parser/template/export types in `src/features/course-assignments/services/course-roster-csv.ts`, related roster types/schemas, workspace file copy, and parser/component tests; support UTF-8/BOM, standard quoting, `name`/`Student Name`, blank-row tolerance, non-empty extra-column rejection, 1–100 rows, 1–200 normalized characters, `INVALID_NAME`, source indexes, and repeated rows. Acceptance: valid files prepare independent source rows and structural failures produce no service call or membership write. Verify: `pnpm vitest run src/__tests__/features/course-assignments/course-roster-csv.test.ts src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): accept official name roster csv`.

- [x] 2.2 Deliver deterministic name evidence as a pure tested module. Scope: add cohesive normalization/token/evidence code under `src/features/course-assignments/services/` and tests for strict NFKC/case/whitespace equality, ordered middle-token omission, initials, separator punctuation, recognized suffixes, diacritic-only suggestions, unique strongest-tier rules, equal-name ambiguity, and no progressive weakening. Acceptance: canonical/source names are never mutated or persisted; suggestions expose closed reason codes and no confidence percentage. Verify: focused matching unit tests, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): add explainable name matching`.

- [x] 2.3 Deliver assignment-authorized batch preview. Scope: add the server preview service/action and candidate read model using existing roster authorization; batch-load eligible Students, current/restorable memberships, conflicts, valid profile/Major, assignment-period placement, and bounded strong-name diagnostics; update types and service/action tests. Acceptance: Program-specific and General Education rules match the specs, irregular Students remain selectable, no N+1 query exists, preview performs zero membership mutations, and Program Head selected-Program scope is preserved. Verify: focused preview, policy, action, and query-shape tests, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): prepare scoped roster preview`.

- [x] 2.4 Deliver bounded shared candidate search for reconciliation and manual add. Scope: add server action/service plus client search interaction; require two normalized characters, top 10, no pagination/browse, same candidate/diagnostic scope as bulk preview, 300 ms debounce, and stale-result suppression. Acceptance: ambiguous and same-name results show authorized disambiguation context; out-of-scope accounts are not disclosed; manual search and bulk resolution share one scope contract. Verify: focused search service/action/component tests, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): add scoped student search`.

## 3. Responsive review and reconciliation

- [x] 3.1 Deliver the three-step responsive workspace using component-session state. Scope: modify `src/features/course-assignments/components/course-roster-management.tsx` and roster detail composition/tests to render Add members, Review and resolve, and Results in the existing desktop Dialog/mobile Drawer; add assignment context, five grouped counts, adaptive row cards, filters, exact/suggested/ambiguous/no-match/invalid states, change/resolve/skip controls, and Already active/Will restore/conflict dispositions. Acceptance: every row stays visible, mobile has no unintended horizontal scroll, required controls are touch/keyboard accessible, and no preview data enters browser storage/cache/database. Verify: focused roster page tests plus runtime desktop/mobile workflow through the project UI verification loop, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): add roster reconciliation workspace`.

- [x] 3.2 Deliver suggestion acknowledgement, duplicate-selection prevention, and discard safety in the workspace. Scope: clear count-aware acknowledgement on suggestion changes; prevent confirmation until unresolved rows are explicitly skipped/resolved and selected IDs are unique; add accessible dirty-preview discard confirmation while preserving pending-action close protection and trigger focus restoration. Acceptance: same-name rows can map to different accounts, one account cannot remain selected twice, and close/refresh behavior matches session-only design. Verify: focused component tests for desktop/mobile, keyboard, pending, discard, repeated rows, and acknowledgement, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): guard roster reconciliation decisions`.

## 4. Identity-based confirmation and manual add

- [x] 4.1 Deliver confirmation request preflight and identity-based single-row mutation seam. Scope: replace email mutation inputs in roster schemas/actions/services with `studentUserId`; validate source indexes, explicit skips, suggestion acknowledgement, duplicate IDs, assignment authorization/mutability, and server-recomputed candidate scope before writes; preserve three-layer Program Head scope validation. Acceptance: crafted/out-of-scope IDs, duplicate IDs, and read-only rosters perform zero writes with safe results. Verify: focused schema/action/policy/mutation security tests, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): confirm scoped student identities`.

- [x] 4.2 Deliver row-wise bulk confirmation with current-state outcomes. Scope: add bounded sequential confirmation using short assignment-lock transactions and existing create/restore/audit/SQL-conflict semantics; continue expected row conflicts, stop unexpected failure with one reference and `UNPROCESSED` remainder; return separate resolution/disposition/final outcomes. Acceptance: concurrency changes are revalidated, prior commits survive unexpected failure, no name rematching/substitution occurs, and no raw database error escapes. Verify: focused confirmation, mutation, constraint, and concurrency tests; database integration tests only against an explicitly disposable database, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): commit reconciled roster rows`.

- [x] 4.3 Deliver manual name search Add and privacy-safe results/export. Scope: remove direct email entry from the workspace; select a scoped candidate then call the shared identity mutation; update results table/cards, grouped summary, support-reference presentation, and failed export to `row,name,status,error` with optional non-email academic context; update logs to omit names/emails. Acceptance: authorized email remains visible only as live disambiguation context; export/logs contain no candidate email, UUID, alternative list, raw error, or row-level support reference. Verify: focused manual-add, result/export, logging, action, and UI tests plus desktop/mobile runtime smoke, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): finish identity based roster management`.

## 5. Evaluation boundary and compatibility verification

- [x] 5.1 Verify Course-bound and central evaluation integration after the roster cutover. Scope: update any remaining evaluation preview/publication/component tests and projections; preserve active `CourseAssignmentMembership.student_user_id` sourcing for Course-bound recipients, exclusions, publication locks, current eligibility, finalized responses, and central deployment enrollment sourcing. Acceptance: no evaluation path reruns name matching or derives Course membership from placement, and no Student ID is displayed or transported. Verify: focused Course-bound preview/publication/late-include/response and central deployment tests, `pnpm lint`, `pnpm build`. Commit: `test(evaluations): preserve roster identity boundary`.

- [x] 5.2 Complete the release-gate A regression and production compatibility evidence. Scope: verify every affected callsite/test/doc, run repository search for obsolete email-import and Student ID contracts, and record that deployed code no longer selects/writes the compatibility column before schema removal. Acceptance: full suite is green apart from documented unrelated failures; runtime name upload/manual add work at representative desktop/mobile sizes; old application versions remain rollback-compatible because the column still exists. Verify: `pnpm test`, `pnpm lint`, `pnpm build`, changed-code static audit, and documented runtime evidence. Commit: `test(course-rosters): verify name resolution cutover`.

## 6. Student ID database contract migration

- [x] 6.1 Deliver the irreversible Student ID schema contract migration after gate A approval. Scope: remove `student_id_number` from `prisma/models/identity-access.prisma`; generate SQL with `pnpm supabase:migration:diff -- remove_student_id_number`; review that it only drops the intended column; run `pnpm supabase:push:dry-run`; apply through the repository deployment workflow; regenerate `src/types/supabase-database.ts` with `pnpm supabase:types`; never edit historical migrations or generated types manually. Acceptance: Prisma and deployed Supabase schema omit the column and existing values are discarded without replacement. Verify: Prisma validation/generation, migration dry-run/application evidence, generated type diff, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(students): drop unverified student id`.

- [x] 6.2 Complete contract cleanup and release documentation. Scope: remove compatibility-only comments/evidence, sync delta specs to main contracts when approved, finalize ADR 0015 status/supersession links and domain glossaries/user journeys, confirm no active Student ID or email-roster contract remains, and record rollback/recovery as forward migration only after column drop. Acceptance: OpenSpec validation passes, active documentation agrees with implementation/schema, and every named requirement has verification evidence. Verify: `openspec validate replace-student-id-with-name-roster-resolution --strict`, `pnpm test`, `pnpm lint`, `pnpm build`. Commit: `docs(course-rosters): finalize roster identity contract`.