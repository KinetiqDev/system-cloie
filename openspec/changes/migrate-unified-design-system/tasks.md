## 1. Semantic foundation vertical slice

- [ ] 1.1 Establish approved Light and Dark semantic token parity without changing existing Light workflow behavior. Scope: record ADR 0009 and the Design System context; replace legacy foundation values, add missing semantic roles, repair Tailwind/shadcn mappings, retokenize root/public shells, and prove core display/form primitives resolve Light and Dark roles. Add the approved Base UI Spinner only after reviewing project-aware shadcn source, then compose it with Button loading. Paths: `docs/adr/0009-protected-design-system-showcase.md`, `CONTEXT-MAP.md`, `src/features/design-system/CONTEXT.md`, `src/styles/tokens.css`, `src/app/globals.css`, `src/app/layout.tsx` metadata and root semantic classes only, `src/app/manifest.ts`, `src/app/(public)/layout.tsx`, `src/components/ui/{button,card,checkbox,input,label,progress,radio-group,select,sheet,skeleton,spinner,switch,table,tabs,textarea}.tsx`, focused `src/__tests__/components/design-system/**` and updated primitive tests. `src/app/global-error.tsx` is owned solely by task 9.4; task 1.1 retokenizes the deprecated `cta-success` branch but task 8.1 alone removes it together with its caller. Acceptance: no legacy gold semantic family, no undefined `bg-surface-hover`, no coupled accent/ring/information/chart roles, exported-or-removed CardAction, and no raw theme value in the listed shared/root paths; standard Button variants preserve current APIs and support disabled Spinner-composed loading without width change or duplicate activation; the temporary `cta-success` resolves semantic success tokens and remains type-safe until task 8.1 removes it with its caller. Verify: focused Vitest primitive/foundation tests including pending Button semantics, raw-value audit scoped to touched paths, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): establish semantic light and dark foundations`.

## 2. Appearance preference vertical slice

- [ ] 2.1 Deliver persisted Light, Dark, and System resolution with first-paint correctness and no user-state reset, while primary Production remains forced Light until the release gate. Scope: add the server-only availability resolver and `CLOIE_APPEARANCE_ENABLED` environment contract, resolve the deployed CSP before implementation, then add the matching narrow bootstrap, provider, selector, Settings Appearance route, and avatar-menu entry; retain root/server rendering, the existing account-state guard, one active account role, and role-owned routes. This task depends on 1.1; in the shared `src/app/layout.tsx` it owns only appearance bootstrap/provider insertion and does not reopen task 1.1 metadata or root semantic-class work. Paths: `.env.example`, `src/proxy.ts` only if CSP nonce propagation is required, `src/features/design-system/{lib/appearance.ts,services/resolve-appearance-availability.ts,components/{appearance-bootstrap-script,appearance-provider,appearance-selector}.tsx}`, `src/app/layout.tsx` appearance insertion only, `src/components/layout/topbar.tsx`, `src/app/(app)/settings/appearance/{page,loading}.tsx`, `src/__tests__/components/design-system/appearance-*.test.tsx`, and exact route tests for Settings Appearance. Acceptance: in development/valid dedicated demo, missing or malformed preference resolves System, explicit Light/Dark overrides OS, and System responds to OS changes; in primary Production without the release setting, the bootstrap forces Light before paint, ignores storage, writes no preference, omits the menu control, and returns not-found for Settings Appearance; the explicit release setting enables the approved control only after task 10.1; the effective production CSP permits the before-paint script or a tested nonce path, otherwise rollout remains unavailable; enabled appearance changes preserve route/form/filter/scroll/async state. Verify: focused availability/appearance/route-guard tests, primary-Production OS-Dark browser evidence, development no-flash and OS-change evidence, deployed-CSP check, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add gated appearance preferences`.

## 3. Protected showcase scaffold vertical slice

- [ ] 3.1 Deliver the authenticated, development/dedicated-demo-only Design System Showcase with real production tokens and static fixtures. Scope: create the server access policy, route, loading state, static reference structure, table of contents, foundations inventory, fixture data, and an ordered section registry that later dependent showcase tasks extend without reopening the route page; do not add navigation to role-primary surfaces or any mutation/data access. Paths: `src/app/(app)/design-system/{layout,page,loading}.tsx`, `src/features/design-system/services/resolve-showcase-access.ts`, `src/features/design-system/data/showcase-fixtures.ts`, `src/features/design-system/components/{design-system-showcase-page,showcase-section,token-reference,component-state-matrix,showcase-section-registry}.tsx`, `src/__tests__/features/design-system/showcase-access.test.ts`, and `src/__tests__/app/design-system-page.test.tsx`. Acceptance: authenticated development and valid dedicated-demo requests render inside the existing AppShell; primary Production and invalid demo configuration return not-found before content; unauthenticated/incomplete accounts retain existing guards; all fixture data is typed, serializable, non-sensitive, and mutation-free; foundations render actual production token classes and primitives; the registry renders scaffold-owned sections through the protected route. Verify: focused access-policy/route tests covering production denial and account gates, `pnpm verify:production-auth-boundary`, `pnpm verify:dedicated-demo-auth-boundary`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add protected showcase scaffold`.

## 4. Shared feedback and interactive showcase vertical slice

- [ ] 4.1 Deliver production feedback/state primitives and their live showcase coverage after tasks 1.1 and 3.1. Scope: add approved Base UI `field.tsx`, `empty.tsx`, and `popover.tsx` only after reviewing the project-aware shadcn source; extend the one existing toast contract with information; retokenize alerts/badges/overlays; add interactive form, overlay, toast, table-selection, feedback, and offline-reference showcase sections. This task consumes the Button/Spinner API from 1.1 and does not reopen either file; it sequentially extends the scaffold-owned `showcase-fixtures.ts` and `showcase-section-registry.ts` from 3.1 with interactive-only fixtures and registrations. Paths: `src/components/ui/{field,empty,popover}.tsx`, `src/components/ui/{alert,alert-dialog,badge,dialog,drawer,dropdown-menu,toast,tooltip}.tsx`, `src/features/design-system/components/{form-controls-showcase,overlay-and-feedback-showcase,showcase-section-registry}.tsx` interactive registrations only, `src/features/design-system/data/showcase-fixtures.ts` interactive additions only, and focused `src/__tests__/components/{ui,design-system}/**` plus the showcase route test. Acceptance: forms show visible labels, helper/error text, valid/invalid/disabled state; Button plus Spinner preserve width and prevent duplicate submission while loading; success/warning/danger/information feedback is semantic and readable; overlays preserve Base UI title/focus/Escape behavior; the protected route renders the feedback/overlay section with real interactive controls; showcase examples execute real component behavior with only local fixture state; offline content is explicitly static reference only. Verify: focused Testing Library keyboard/focus/form/toast and showcase-route tests, browser keyboard trace, Lighthouse accessibility audit of the showcase, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add shared feedback and state coverage`.

## 5. Accessible chart vertical slice

- [ ] 5.1 Remove raw qualitative response text from the interactive Faculty analytics chart DTO before changing chart visuals. Scope: make the server compute word-frequency tokens, response counts, and concise summaries; remove `qualitativeTexts` from the client-facing type and Client Component aggregation; preserve independently authorized detailed response-review behavior. Paths: `src/features/analytics/services/get-faculty-analytics-data.ts`, `src/features/analytics/types.ts`, `src/lib/actions/faculty-analytics-actions.ts`, `src/features/analytics/components/{faculty-analytics-dashboard,faculty-qualitative-cloud}.tsx`, `src/app/(app)/faculty/analytics/page.tsx`, and `src/__tests__/modules/analytics-reporting-and-review/**` plus a new exact client-DTO regression test. Acceptance: selecting Faculty Course-bound evaluations serializes only display metadata, quantitative aggregates, word-frequency tokens, and counts required by the dashboard; client DTOs exclude `qualitativeTexts`, text content, response rows, respondent identifiers, and account emails; Faculty authorization and detailed response-review routes remain unchanged. Verify: focused service/action/component payload tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `fix(analytics): keep qualitative responses server-side`.

- [ ] 5.2 Deliver theme-resolved Recharts presentation and migrate the current analytics visualizations after tasks 3.1, 4.1, and 5.1. Scope: add the Base UI-compatible shadcn chart wrapper, migrate raw color arrays and chart support styling, add deterministic non-color differentiation for repeated categories, and give every chart a legend, value access, text insight, and tabular or equivalent accessible summary while retaining existing server authorization and narrowed client chart boundaries. It sequentially extends the scaffold-owned `showcase-section-registry.ts` with only the chart registration. Paths: `src/components/ui/chart.tsx`, `src/features/analytics/components/{course-mean-pie-chart,faculty-cilo-analytics-chart,mean-bar-chart,stakeholder-mean-pie-chart,qualitative-word-cloud,faculty-dashboard-visualizations,faculty-dashboard-visualization-fallbacks}.tsx`, `src/features/design-system/components/{chart-showcase,showcase-section-registry}.tsx` chart registration only, and `src/__tests__/components/{analytics,faculty-dashboard-visualization-fallbacks,design-system}/**` plus the showcase route test. Acceptance: all listed charts consume `--chart-*` and support tokens rather than raw palettes; five categorical roles resolve in both appearances; more-than-five category rendering has text/marker/pattern distinction; empty/loading/error states are distinct; screen readers can identify the chart insight and obtain displayed values; client chart props retain task 5.1 confidentiality constraints; the protected route renders the chart section and its accessible summary. Verify: focused chart and showcase-route tests for palette configuration, empty state, accessible summary, and repeated-category distinction; browser desktop/mobile Light/Dark screenshots; `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(analytics): migrate charts to semantic palettes`.

## 6. Navigation and shared status vertical slice

- [ ] 6.1 Retokenize role-aware navigation without changing navigation information architecture. Scope: repair semantic hover/active/focus styles in desktop sidebar, Dean tablet rail, mobile drawer, and respondent bottom navigation; normalize student desktop/mobile labels only where approved; retain central navigation ownership and mobile mode rules. Later domain slices own status/badge content inside their pages and consume this navigation work without reopening it. Paths: `src/components/layout/{app-shell,sidebar,mobile-sidebar-drawer,mobile-nav,navigation-link}.tsx`, `src/lib/constants/navigation.ts`, and `src/__tests__/components/{mobile-nav,mobile-sidebar-drawer,navigation-link,design-system}/**`. Acceptance: active navigation uses selected/primary tokens, hover resolves through defined surface roles, all interactive navigation retains visible focus and `aria-current`, tablet/mobile substitutions retain current role rules, and no role-primary item is added for the showcase. Verify: focused navigation tests at role and viewport boundaries, keyboard/tap-target browser checks, raw-value audit scoped to touched paths, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(navigation): apply semantic navigation roles`.

## 7. Administrative domain vertical slices

- [ ] 7.1 Migrate Secretary user-management data display and edit dialogs to semantic status, form, table, and confirmation patterns after navigation task 6.1. Scope: remove the three duplicated role-color helpers and raw success values while preserving account creation, protected account edit, active/inactive state, and server authorization behavior. Paths: `src/features/users/components/{stat-cards,secretary-add-user-form}.tsx`, `src/features/users/components/secretary-users-list/{index,users-data-table,user-dialogs,edit-user-dialog,users-filter-bar,users-pagination,users-kpi}.tsx`, `src/app/(app)/secretary/users/page.tsx`, `src/__tests__/components/secretary-users-list.test.tsx`, and `src/__tests__/components/secretary-add-user-form.test.tsx`. Acceptance: role/category badges, lifecycle feedback, tables, labels, and dialogs resolve semantic roles in Light/Dark; protected account edits retain exact review/confirmation behavior; no raw palette class remains in the listed files. Verify: focused user component/route tests, desktop/mobile Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(users): apply semantic management UI`.

- [ ] 7.2 Migrate Academic Calendar management surfaces to shared semantic field, badge, dialog, loading, and error patterns. Scope: visual-only conversion of school-year and active academic period presentation; preserve school year, semester, academic term, Summer semester, assignment period, and active academic period behavior. Paths: `src/features/academic-calendar/components/{active-term-badge,rollover-exceptions-table,rollover-runner,school-year-form,school-year-list,set-active-term-dialog,term-instance-form,term-instance-picker}.tsx`, `src/app/(app)/secretary/school-years/page.tsx`, `src/app/(app)/secretary/school-years/[id]/client-page.tsx`, and existing academic-calendar component tests. Acceptance: lifecycle/status/empty/loading/error states are semantic and accessible in both appearances; no term lifecycle rule, mutation, or route state changes. Verify: focused academic-calendar tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-calendar): align management UI with tokens`.

- [ ] 7.3 Migrate Academic Structure program/course management surfaces to semantic cards, badges, forms, dialogs, and tables. Scope: preserve program lifecycle steward authority, program deactivation, program deletion, major management, and Course catalog behavior while removing identified raw category/lifecycle values. Paths: `src/features/academic-structure/components/{secretary-programs-list,program-form,management-courses-list,course-form,manage-majors-dialog,program-head-courses-catalog}.tsx`, `src/app/(app)/secretary/{programs,courses}/page.tsx`, `src/app/(app)/program-head/courses/page.tsx`, `src/app/(app)/dean/academic-structure/{programs,courses}/page.tsx`, and existing academic-structure tests. Acceptance: active/inactive/category representations use approved semantic/categorical roles without changing program deletion/deactivation or role scope; listed pages retain responsive table containment and dialog/drawer behavior. Verify: focused academic-structure tests, Light/Dark desktop/mobile browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-structure): migrate management UI to semantic tokens`.

- [ ] 7.4 Migrate Course Assignment and enrollment management displays to shared semantic tables, alerts, filters, status, and confirmation patterns. Scope: preserve Course Assignment, Course-assignment membership, roster eligibility, role-owned route, and all-program Course assignment manager invariants while replacing the identified raw warning/success/danger values. Paths: `src/features/course-assignments/components/{course-assignment-form-dialog,course-assignments-page-shell,course-assignments-table,course-roster-management,course-roster-pages,course-row-assignments-sheet,edit-course-assignment-dialog}.tsx`, `src/features/course-assignments/components/shared/{assignment-filters,assignment-summary-block,class-identity-fields,faculty-search-popover,wizard-stepper}.tsx`, `src/features/enrollments/components/{enrollment-editor-dialog,student-enrollment-history}.tsx`, `src/app/(app)/{secretary/course-assignments,dean/academic-structure/course-assignments,program-head/course-assignments,faculty/course-rosters}/page.tsx`, and existing course-assignment/enrollment tests. Acceptance: assignment and membership lifecycle messages preserve safe domain wording and controls; wide tables stay contained; forms retain visible label/error semantics; no data/service/authorization behavior changes. Verify: focused course-assignment/enrollment tests, disposable database suite only if an invariant test changes, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(course-assignments): apply semantic operational states`.

## 8. Authoring and evaluation domain vertical slices

- [ ] 8.1 Migrate instrument and template authoring UI to semantic action, field, feedback, and loading patterns after task 1.1. Scope: replace raw danger/success and hardcoded action treatments while retaining drag-and-drop, template access, publishing, Server Actions, and `customZodResolver` behavior; remove the deprecated `cta-success` branch from `button.tsx` in the same commit as its only `template-builder.tsx` caller moves to the approved action hierarchy. Paths: `src/features/instruments/components/{faculty-tools-page,management-template-builder,management-tools-page,program-head-template-builder,program-head-tools-page,template-builder,templates-grid}.tsx`, `src/components/ui/button.tsx` `cta-success` branch only, `src/app/(app)/{secretary/instruments,faculty/tools,program-head/tools}/page.tsx`, and existing instrument/template-builder component tests. Acceptance: routine secondary actions remain neutral, specialized cyan actions are separately named, the former `cta-success` caller uses the approved replacement variant, destructive controls retain confirmation/recovery semantics, and no raw palette class remains in the listed files. Verify: focused instrument/template-builder tests including replacement action rendering, keyboard/reduced-motion Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(instruments): align authoring UI with semantic tokens`.

- [ ] 8.2 Migrate Course-bound evaluation deployment and review controls to semantic lifecycle, validation, confirmation, and status treatment. Scope: preserve Course-bound evaluation publication, recipient exclusion, roster lock, eligibility, and close behavior while retokenizing identified informational, warning, destructive, and page-level status UI. Paths: `src/features/evaluations/components/{assignment-picker,close-evaluation-dialog,evaluation-detail-dialog,faculty-cilos-course-list,faculty-published-evaluations-table,index,late-include-dialog,publish-central-deployment-form,publish-course-bound-evaluation-form-v2}.tsx`, `src/app/(app)/faculty/cilo-evaluations/new/page.tsx`, `src/app/(app)/faculty/cilo-evaluations/[evaluationId]/page.tsx`, and existing faculty/course-bound review tests. Acceptance: validation and lifecycle notices state cause and recovery with non-color cues; confirmed destructive actions retain safe cancellation; all listed forms keep current Server Action and authorization behavior. Verify: focused evaluation tests, keyboard/overlay Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(evaluations): apply semantic deployment states`.

- [ ] 8.3 Migrate Program Head outcome authoring to shared semantic patterns without altering outcome ownership. Scope: preserve Graduate Outcome ownership, Course-level CILO behavior, and CILO-to-GO mapping rules while converting only outcome-authoring presentation. Paths: `src/features/outcomes/components/{go-form-dialog,program-head-outcomes-page}.tsx`, `src/app/(app)/program-head/outcomes/page.tsx`, and existing outcomes tests. Acceptance: authoring, warning, empty, loading, error, and confirmation states use semantic tokens and visible focus without changing role/program scope, outcome ownership, or mapping rules. Verify: focused outcomes tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(outcomes): migrate authoring UI`.

## 9. Public and respondent vertical slices

- [ ] 9.1 Migrate the role selection portal, public login/onboarding/account-status, and public form components to the shared design system. Scope: preserve public entry as the Role selection portal, Google-authenticated account flow, self-service role claim, and account-status behavior without exposing showcase or demo controls. Paths: `src/features/portals/components/{hero-card,portal-choice-card,portal-shell,role-selection-card,session-banner}.tsx`, `src/features/users/components/{alumni-onboarding-form,faculty-onboarding-form,industry-partner-onboarding-form}.tsx`, `src/app/(public)/{login,onboarding,portal,status}/page.tsx`, `src/app/(public)/onboarding/student-profile-form.tsx`, `src/__tests__/app/{portal-respondents-page,staff-portal-page}.test.tsx`, and existing onboarding tests. Acceptance: low-density public pattern, visible labels, helper/error recovery, and responsive role cards remain intact in both appearances; no public route renders the protected showcase, demo switcher, or unavailable appearance control. Verify: focused public/onboarding tests, Light/Dark mobile/desktop browser checks, public form accessibility audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(portal): apply semantic public entry UI`.

- [ ] 9.2 Migrate respondent response/review flow presentation to semantic progress, form, warning, dialog, and loading states. Scope: preserve evaluation response submission behavior, guided low-density wizard layout, mobile comfort, and respondent eligibility while retokenizing the identified review warning. Paths: `src/features/responses/components/{review-modal,submitted-response-review,wizard-shell}.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/page.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/[id]/page.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/[id]/submitted/page.tsx`, and existing response component tests. Acceptance: progress has text/count support, errors explain recovery, mobile controls meet touch targets, and no response payload, submission transition, or eligibility decision changes. Verify: focused response tests, mobile Light/Dark keyboard/touch browser checks, respondent-flow accessibility audit, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(responses): align respondent flow with semantic tokens`.

- [ ] 9.3 Migrate respondent profile and history data presentation to approved type, status, table, and empty-state rules after navigation task 6.1. Scope: replace raw completed badges and text below the approved minimum without changing profile data or submission history. Paths: `src/app/(app)/{student,alumni,industry-partner}/{profile,history}/page.tsx`, and existing profile/history tests. Acceptance: history status remains explicitly labeled Completed, profile metadata uses approved type utilities, tables are contained on mobile, and the existing bottom-navigation route set/active behavior remains unchanged. Verify: focused respondent profile/history tests, 375px/768px Light/Dark browser checks, scoped text-size/raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(respondents): migrate profile and history UI`.

- [ ] 9.4 Migrate legal, global error, not-found, unauthorized, and route-error presentation to semantic tokens without changing public/legal routing. Scope: replace the raw global-error inline palette and retokenize formal legal callouts/navigation while preserving legal acknowledgement and safe error recovery. Paths: `src/features/legal/components/{legal-acknowledgement-dialog,legal-document-content,legal-document-nav,legal-footer,legal-page-header,legal-page-shell,legal-section,mobile-legal-document-nav}.tsx`, `src/app/(legal)/privacy/page.tsx`, `src/app/(legal)/terms/page.tsx`, `src/app/{error,global-error,not-found}.tsx`, `src/app/unauthorized/page.tsx`, `src/components/error-boundary.tsx`, `src/components/layout/{operational-route-error,respondent-route-error}.tsx`, and existing legal/error tests. Acceptance: legal prose remains readable, error states state the failure and recovery, global error uses semantic Light/Dark-safe presentation, and no public/legal navigation or acknowledgement behavior changes. Verify: focused legal/error tests, Light/Dark browser checks, accessibility audit of error and legal pages, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(legal): apply unified semantic error and legal UI`.

## 10. Completion and controlled production activation vertical slice

- [ ] 10.1 Complete the cross-repository visual, accessibility, and deployment-boundary acceptance gate, then enable `CLOIE_APPEARANCE_ENABLED` for primary Production. Scope: remove resolved known-gap rows from `docs/design.md`, refresh the showcase inventory and project design documentation, establish an auditable allowlist for legitimate raw asset/logo values only, verify all completed role/page slices, and activate the server-only release setting after every prior task passes. Paths: `.env.example`, `docs/design.md`, `docs/adr/0009-protected-design-system-showcase.md` if final decision wording changes, `src/features/design-system/{data/showcase-fixtures.ts,services/resolve-appearance-availability.ts}`, exact design-system tests, and documented deployment evidence permitted by repository policy. Acceptance: all approved component categories and required states are represented by production sources in the showcase; Light/Dark/System passes at 375px, 768px, 1024px, and 1440px; keyboard focus, contrast, touch target, reduced motion, chart accessibility, loading/empty/error/offline-reference, role-aware navigation, first-paint behavior, and client-chart DTO privacy pass; raw color/palette audit has no unreviewed component violations; the showcase remains unavailable in primary Production; before configuration activation, primary Production forces Light even for OS-Dark visitors, and after activation it exposes the standard appearance selector without changing any product route. Verify: all focused Vitest suites, `pnpm lint`, `pnpm test`, `pnpm build`, Lighthouse accessibility for showcase plus representative public/operational/respondent routes, Chrome DevTools Light/Dark/System visual matrix, effective production CSP check, `pnpm verify:production-auth-boundary`, `pnpm verify:dedicated-demo-auth-boundary`, and `pnpm verify:demo-target-isolation`. Commit: `feat(design-system): complete unified appearance migration`.
