## 1. Rebaseline, Ownership, ADR 0010, and CSP Feasibility

- [ ] 1.1 Record ADR 0010, reconcile navigation and analytics ownership with the concurrent `improve-navigation-rendering-and-caching` change, and establish the rebaselined inventory. Scope: create `docs/adr/0010-unified-appearance-and-protected-showcase.md`, `CONTEXT-MAP.md` entry, and `src/features/design-system/CONTEXT.md`; verify that navigation deepest-match, pending, `aria-current`, focus containment, and responsive defect behaviors remain owned by the concurrent rendering change; verify that Faculty analytics DTO privacy narrowing is assigned to `improve-navigation-rendering-and-caching` task 4.5. This task does not edit production code; it establishes the design-system context and confirms ownership boundaries. Paths: new `docs/adr/0010-unified-appearance-and-protected-showcase.md`, `CONTEXT-MAP.md`, new `src/features/design-system/CONTEXT.md`. Acceptance: ADR 0010 references `docs/design.md`, ADR 0006, ADR 0008, and ADR 0009 without duplicating their specifications; no ADR numbering collision; navigation and analytics DTO ownership are documented with one owning change each. Verify: `pnpm lint`, `pnpm build`. Commit: `docs(design-system): record unified appearance ADR and ownership boundaries`.

- [ ] 1.2 Inspect effective deployment Content Security Policy headers and determine the first-paint appearance bootstrap mechanism. Scope: compare four mechanisms against the version-matched Next.js 16.2 CSP documentation — inline bootstrap with `'unsafe-inline'`, same-origin external bootstrap, hash-based SRI (experimental), and nonce-based CSP through `src/proxy.ts`. Document that nonce CSP forces all pages to dynamic rendering, removes static/CDN benefits, and is incompatible with Partial Prerendering. Record the selected mechanism in the durable artifacts; appearance implementation must not proceed until the decision is recorded there. Paths: research-only for code — the task may inspect `src/proxy.ts`, `next.config.ts`, and the effective deployment headers but adds no CSP configuration; durable outputs are `docs/adr/0010-unified-appearance-and-protected-showcase.md` (the Decision statement and section 4 "CSP and first-paint mechanism") and `openspec/changes/migrate-unified-design-system/design.md` (Decision 2 and the Risks/Trade-offs CSP entry). Acceptance: the decision recorded in ADR 0010 and `design.md` selects one bootstrap mechanism and names its exact affected paths, first-paint behavior, CSP requirements, rendering and performance consequences, and rollback; the recorded decision accounts for the existing `src/proxy.ts` session-refresh flow and the streamed `Suspense` root layout; nonce CSP is not a casual fallback. Verify: `openspec validate migrate-unified-design-system`; document the findings in the task evidence and the durable artifacts; `pnpm lint`, `pnpm build`. Commit: `docs(design-system): record CSP feasibility decision`.

## 2. Semantic Token and Tailwind/shadcn Mapping Parity

- [ ] 2.1 Establish approved Light and Dark semantic token parity in `src/styles/tokens.css` and `src/app/globals.css`. Scope: replace legacy gold secondary family and light-only values with the approved Light and Dark token values from `docs/design.md` sections 5.2–5.5; add the missing `--color-surface-hover`; decouple neutral `accent`, dedicated `ring`, indigo `information`, neutral `secondary`, ACD `brand-accent`, and categorical `chart-*`; add popover, input, overlay, and selection support tokens; map `sidebar-*` to navigation roles; replace the source comment from `docs/design-system.txt` to `docs/design.md`. This task changes token definitions and Tailwind/shadcn mappings only; it does not retokenize components. Paths: `src/styles/tokens.css`, `src/app/globals.css`. Acceptance: no legacy gold semantic family; no undefined `bg-surface-hover`; no coupled accent/ring/information/chart roles; Light token values match `docs/design.md` section 5.2; Dark token values are present under `.dark` selectors. Verify: focused Vitest token parity tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): establish semantic light and dark token foundations`.

## 3. Root Metadata and Public-Shell Semantic Conversion

- [ ] 3.1 Retokenize root metadata, public shell, and manifest color values. Scope: replace `#0051C3` in `src/app/manifest.ts` and `src/app/layout.tsx` viewport `themeColor` with the approved operational primary; retokenize `src/app/(public)/layout.tsx` with semantic classes. This task preserves the current streamed `AuthenticatedAppShell` under `Suspense` and does not reopen the auth shell boundary. Paths: `src/app/layout.tsx` (viewport metadata only), `src/app/manifest.ts`, `src/app/(public)/layout.tsx`. Acceptance: metadata and public shell use approved semantic roles; no raw `#0051C3` remains in metadata declarations; public shell renders correctly in Light and Dark. Verify: focused metadata/public-shell tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): convert root metadata and public shell to semantic tokens`.

## 4. Existing Shared Primitive Retokenization and Spinner Addition

- [ ] 4.1 Retokenize existing shared UI primitives and add the reviewed Spinner. Scope: retokenize `button.tsx`, `card.tsx`, `checkbox.tsx`, `input.tsx`, `label.tsx`, `progress.tsx`, `radio-group.tsx`, `select.tsx`, `sheet.tsx`, `skeleton.tsx`, `switch.tsx`, `table.tsx`, `tabs.tsx`, `textarea.tsx`, `field.tsx`, and `empty.tsx` to use semantic token classes; export `CardAction` from `card.tsx`; retain the deprecated `cta-success` branch with semantic success tokens (its removal happens in task 12.1 with its caller); add `spinner.tsx` after reviewing project-aware shadcn source through dry-run; compose the Spinner with Button loading, preserving label/width and preventing duplicate submission. This task depends on 2.1. Paths: `src/components/ui/{button,card,checkbox,input,label,progress,radio-group,select,sheet,skeleton,spinner,switch,table,tabs,textarea,field,empty}.tsx`, and focused `src/__tests__/components/design-system/**` plus updated primitive tests. Acceptance: no raw theme value in the listed shared paths; standard Button variants preserve current APIs and support disabled Spinner-composed loading without width change or duplicate activation; `CardAction` is exported; the temporary `cta-success` resolves semantic success tokens and remains type-safe until task 12.1; `spinner.tsx` is added after project-aware dry-run inspection confirms Base UI source and no Radix dependency. Verify: focused Vitest primitive/foundation tests including Button semantics, raw-value audit scoped to touched paths, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): retokenize shared primitives and add spinner`.

## 5. Appearance Bootstrap, Provider, Storage, and Availability Gate

- [ ] 5.1 Deliver persisted Light, Dark, and System resolution with first-paint correctness after the task 1.2 CSP feasibility decision is recorded in ADR 0010 and `design.md`. Scope: implement the server-only availability resolver, `CLOIE_APPEARANCE_ENABLED` environment contract, the narrow bootstrap script, provider, selector, Settings Appearance route, and avatar-menu entry using the mechanism selected by task 1.2 and recorded in those artifacts. This task depends on tasks 2.1 and 1.2; in `src/app/layout.tsx` it owns only appearance bootstrap/provider insertion. Paths: `.env.example`, `src/proxy.ts` only if nonce propagation is required by task 1.2 decision, `src/features/design-system/{lib/appearance.ts,services/resolve-appearance-availability.ts,components/{appearance-bootstrap-script,appearance-provider,appearance-selector}.tsx}`, `src/app/layout.tsx` (appearance insertion only), `src/components/layout/topbar.tsx`, `src/app/(app)/settings/appearance/{page,loading}.tsx`, `src/__tests__/components/design-system/appearance-*.test.tsx`, and route tests for Settings Appearance. Acceptance: in development and valid dedicated demo, missing or malformed preference resolves System, explicit Light/Dark overrides OS, and System responds to OS changes; in primary Production without `CLOIE_APPEARANCE_ENABLED`, the bootstrap forces Light before paint, ignores storage, writes no preference, omits the menu control, and returns not-found UI for Settings Appearance; enabled appearance changes preserve route/form/filter/scroll/async state; appearance storage carries no authorization value. Verify: focused availability/appearance/route-guard tests, primary-Production OS-Dark browser evidence confirming forced Light, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add appearance provider and rollout gate`.

## 6. Protected Showcase Scaffold with Static Fixtures

- [ ] 6.1 Deliver the authenticated, development/dedicated-demo-only Design System Showcase with real production tokens and static fixtures. Scope: create the server access policy, route, loading state, static reference structure, table of contents, foundations inventory, fixture data, and an ordered section registry that later dependent showcase tasks extend without reopening the route page. This task depends on task 2.1 (tokens). Paths: `src/app/(app)/design-system/{layout,page,loading}.tsx`, `src/features/design-system/services/resolve-showcase-access.ts`, `src/features/design-system/data/showcase-fixtures.ts`, `src/features/design-system/components/{design-system-showcase-page,showcase-section,token-reference,component-state-matrix,showcase-section-registry}.tsx`, `src/__tests__/features/design-system/showcase-access.test.ts`, and `src/__tests__/app/design-system-page.test.tsx`. Acceptance: authenticated development and valid dedicated-demo requests render inside the existing `AppShell`; primary Production and invalid demo configuration return not-found UI before content; unauthenticated/incomplete accounts retain existing guards; all fixture data is typed, serializable, non-sensitive, and mutation-free; foundations render actual production token classes and primitives; the registry renders scaffold-owned sections; no role-primary navigation item is added for the showcase. Verify: focused access-policy/route tests covering production denial and account gates, `pnpm verify:production-auth-boundary`, `pnpm verify:dedicated-demo-auth-boundary`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add protected showcase scaffold`.

## 7. Feedback, Form, Overlay, Empty, Loading, and Offline-Reference Showcase Coverage

- [ ] 7.1 Retokenize feedback, overlay, and form primitives and add their live showcase coverage. Scope: extend the one existing toast contract with information; retokenize `alert.tsx`, `alert-dialog.tsx`, `badge.tsx`, `dialog.tsx`, `drawer.tsx`, `dropdown-menu.tsx`, `toast.tsx`, and `tooltip.tsx`; add interactive form, overlay, popover, toast, table-selection, feedback, and offline-reference showcase sections; add `src/components/ui/popover.tsx` as a required primitive from the approved `base-nova` registry item verified project-aware to generate Base UI-only source with no Radix dependency and no package addition. This task consumes the Button/Spinner API from 4.1; it sequentially extends scaffold-owned `showcase-fixtures.ts` and `showcase-section-registry.ts` from 6.1. Paths: `src/components/ui/{alert,alert-dialog,badge,dialog,drawer,dropdown-menu,toast,tooltip,popover}.tsx`, `src/features/design-system/components/{form-controls-showcase,overlay-and-feedback-showcase,showcase-section-registry}.tsx` (registrations only), `src/features/design-system/data/showcase-fixtures.ts` (additions only), and focused `src/__tests__/components/{ui,design-system}/**`. Acceptance: forms show visible labels, helper/error text, valid/invalid/disabled state; Button plus Spinner preserve width and prevent duplicate submission while loading; success/warning/danger/information feedback is semantic and readable; overlays preserve Base UI title/focus/Escape behavior; the protected route renders the feedback/overlay section with real interactive controls; showcase examples execute real component behavior with only local fixture state; offline content is explicitly static reference only; `popover.tsx` is added from the approved `base-nova` registry source, imports only `@base-ui/react/popover`, adds no package dependency, and renders live in the showcase overlay section with Base UI title/focus/Escape behavior. Verify: focused Testing Library keyboard/focus/form/toast tests, browser keyboard trace, Lighthouse accessibility audit of the showcase, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add shared feedback and state showcase coverage`.

## 8. External Prerequisite: Faculty Analytics Aggregate DTO Privacy Invariant

Task 9.1 depends on the Faculty Analytics aggregate DTO privacy invariant completed by `improve-navigation-rendering-and-caching` task 4.5, which owns all implementation and verification: the server computes only word-frequency tokens, response counts, and concise summaries, and the client-facing `FacultyAnalyticsData` type excludes `qualitativeTexts`, raw response rows, respondent identifiers, and account emails. This change does not own any part of that implementation and has no task or commit for it; task 9.1 consumes the completed invariant and its chart-DTO exclusion tests verify the dependency.

## 9. Reviewed Chart Wrapper and Analytics Visualization Migration

- [ ] 9.1 Deliver theme-resolved Recharts presentation and migrate current analytics visualizations after the Faculty Analytics aggregate DTO privacy invariant from `improve-navigation-rendering-and-caching` task 4.5 and tasks 6.1/7.1 showcase and primitive foundations. Scope: add the shadcn-compatible `chart.tsx` wrapper after project-aware dry-run inspection — the dry-run proposes overwriting `card.tsx` and a Recharts dependency entry; neither overwrite nor unaudited dependency change is allowed. Migrate raw color arrays to `--chart-*` tokens; add deterministic non-color differentiation for repeated categories; give every chart a legend, value access, text insight, and tabular or equivalent accessible summary. Sequentially extend the scaffold-owned `showcase-section-registry.ts` with chart registration and add `chart-showcase.tsx`. Paths: `src/components/ui/chart.tsx`, `src/features/analytics/components/{course-mean-pie-chart,faculty-cilo-analytics-chart,mean-bar-chart,stakeholder-mean-pie-chart,qualitative-word-cloud,faculty-dashboard-visualizations,faculty-dashboard-visualization-fallbacks}.tsx`, `src/features/design-system/components/{chart-showcase,showcase-section-registry}.tsx` (chart registration only), and `src/__tests__/components/{analytics,faculty-dashboard-visualization-fallbacks,design-system}/**`. Acceptance: all listed charts consume `--chart-*` and support tokens; five categorical roles resolve in both appearances; more-than-five category rendering has text/marker/pattern distinction; empty/loading/error states are distinct; screen readers can identify the chart insight and obtain displayed values; client chart props exclude raw qualitative text per the task 4.5 privacy invariant; the protected route renders the chart section and its accessible summary; `card.tsx` is not overwritten. Verify: focused chart and showcase-route tests for palette configuration, empty state, accessible summary, and repeated-category distinction; browser desktop/mobile Light/Dark screenshots; `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(analytics): migrate charts to semantic palettes`.

## 10. Navigation Visual Retokenization Only

- [ ] 10.1 Retokenize role-aware navigation surfaces without changing navigation information architecture or matching behavior. Scope: repair semantic hover/active/focus styles in desktop sidebar, Dean tablet rail, mobile drawer, and respondent bottom navigation. This task consumes current `getDeepestMatchingNavItem` matching, pending behavior, `aria-current`, and responsive navigation modes from the concurrent rendering change without reopening them. Add `navigation-showcase.tsx` and `responsive-showcase.tsx` entries to the showcase registry. Paths: `src/components/layout/{app-shell,sidebar,mobile-sidebar-drawer,mobile-nav,navigation-link}.tsx`, `src/lib/constants/navigation.ts` (visual-only, no route changes), `src/features/design-system/components/{navigation-showcase,responsive-showcase,showcase-section-registry}.tsx` (registrations only), and `src/__tests__/components/{mobile-nav,mobile-sidebar-drawer,navigation-link,design-system}/**`. Acceptance: active navigation uses selected/primary tokens; hover resolves through defined surface roles; all interactive navigation retains visible focus; tablet/mobile substitutions retain current role rules; no role-primary item is added for the showcase; `navigation-showcase.tsx` renders real navigation declarations and layout seams; logo treatment uses the approved light brand-safe plate in Dark mode. Verify: focused navigation tests at role and viewport boundaries, keyboard/tap-target browser checks, raw-value audit scoped to touched paths, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(navigation): apply semantic navigation roles`.

## 11. Administrative Domain Visual Slices

- [ ] 11.1 Migrate Secretary user-management data display and edit dialogs to semantic status, form, table, and confirmation patterns. Scope: remove the three duplicated role-color helpers and raw success values while preserving account creation, protected account edit, active/inactive state, and server authorization behavior. Paths: `src/features/users/components/{stat-cards,secretary-add-user-form}.tsx`, `src/features/users/components/secretary-users-list/{index,users-data-table,user-dialogs,edit-user-dialog,users-filter-bar,users-pagination,users-kpi}.tsx`, `src/app/(app)/secretary/users/page.tsx`, `src/__tests__/components/secretary-users-list.test.tsx`, and `src/__tests__/components/secretary-add-user-form.test.tsx`. Acceptance: role/category badges, lifecycle feedback, tables, labels, and dialogs resolve semantic roles in Light/Dark; protected account edits retain exact review/confirmation behavior; no raw palette class remains. Verify: focused user component/route tests, desktop/mobile Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(users): apply semantic management UI`.

- [ ] 11.2 Migrate Academic Calendar management surfaces to shared semantic field, badge, dialog, loading, and error patterns. Scope: visual-only conversion; preserve school year, semester, academic term, Summer semester, assignment period, and active academic period behavior. Paths: `src/features/academic-calendar/components/{active-term-badge,rollover-exceptions-table,rollover-runner,school-year-form,school-year-list,set-active-term-dialog,term-instance-form,term-instance-picker}.tsx`, `src/app/(app)/secretary/school-years/page.tsx`, `src/app/(app)/secretary/school-years/[id]/client-page.tsx`, and existing academic-calendar component tests. Acceptance: lifecycle/status/empty/loading/error states are semantic and accessible in both appearances. Verify: focused academic-calendar tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-calendar): align management UI with tokens`.

- [ ] 11.3 Migrate Academic Structure program/course management surfaces to semantic cards, badges, forms, dialogs, and tables. Scope: preserve program lifecycle steward authority, program deactivation, program deletion, major management, and Course catalog behavior. Paths: `src/features/academic-structure/components/{secretary-programs-list,program-form,management-courses-list,course-form,manage-majors-dialog,program-head-courses-catalog}.tsx`, `src/app/(app)/secretary/{programs,courses}/page.tsx`, `src/app/(app)/program-head/programs/[programId]/courses/page.tsx`, `src/app/(app)/dean/academic-structure/{programs,courses}/page.tsx`, and existing academic-structure tests. Acceptance: category representations use approved semantic roles; Program Head route uses canonical `programs/[programId]` path. Verify: focused academic-structure tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-structure): migrate management UI to semantic tokens`.

- [ ] 11.4 Migrate Course Assignment and enrollment management displays to shared semantic tables, alerts, filters, status, and confirmation patterns. Scope: preserve Course Assignment, membership, roster eligibility, role-owned route, and all-program Course assignment manager invariants. Paths: `src/features/course-assignments/components/{course-assignment-form-dialog,course-assignments-page-shell,course-assignments-table,course-roster-management,course-roster-pages,course-row-assignments-sheet,edit-course-assignment-dialog}.tsx`, `src/features/course-assignments/components/shared/{assignment-filters,assignment-summary-block,class-identity-fields,faculty-search-popover,wizard-stepper}.tsx`, `src/features/enrollments/components/{enrollment-editor-dialog,student-enrollment-history}.tsx`, `src/app/(app)/{secretary/course-assignments,dean/academic-structure/course-assignments,program-head/programs/[programId]/course-assignments,faculty/course-rosters}/page.tsx`, and existing tests. Acceptance: lifecycle messages preserve safe domain wording; wide tables stay contained; Program Head route uses canonical `programs/[programId]` path. Verify: focused course-assignment/enrollment tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(course-assignments): apply semantic operational states`.

## 12. Authoring and Evaluation Domain Visual Slices

- [ ] 12.1 Migrate instrument and template authoring UI to semantic action, field, feedback, and loading patterns. Scope: replace raw danger/success and hardcoded action treatments; remove the deprecated `cta-success` branch from `button.tsx` in the same commit as its only `template-builder.tsx` caller moves to the approved action hierarchy. Paths: `src/features/instruments/components/{faculty-tools-page,management-template-builder,management-tools-page,program-head-template-builder,program-head-tools-page,template-builder,templates-grid}.tsx`, `src/components/ui/button.tsx` (`cta-success` branch removal only), `src/app/(app)/{secretary/instruments,faculty/tools,program-head/programs/[programId]/tools}/page.tsx`, and existing tests. Acceptance: routine secondary actions remain neutral; specialized `brand-accent` actions are separately named; the former `cta-success` caller uses an approved replacement; destructive controls retain confirmation/recovery semantics; `cta-success` is removed; Program Head routes use canonical `programs/[programId]` paths. Verify: focused instrument/template-builder tests, keyboard/reduced-motion Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(instruments): align authoring UI with semantic tokens`.

- [ ] 12.2 Migrate Course-bound evaluation deployment and review controls to semantic lifecycle, validation, confirmation, and status treatment. Scope: preserve evaluation publication, recipient exclusion, roster lock, eligibility, and close behavior. Paths: `src/features/evaluations/components/{assignment-picker,close-evaluation-dialog,evaluation-detail-dialog,faculty-cilos-course-list,faculty-published-evaluations-table,index,late-include-dialog,publish-central-deployment-form,publish-course-bound-evaluation-form-v2}.tsx`, `src/app/(app)/faculty/cilo-evaluations/new/page.tsx`, `src/app/(app)/faculty/cilo-evaluations/[evaluationId]/page.tsx`, and existing tests. Acceptance: validation and lifecycle notices state cause and recovery; confirmed destructive actions retain safe cancellation. Verify: focused evaluation tests, keyboard/overlay Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(evaluations): apply semantic deployment states`.

- [ ] 12.3 Migrate Program Head outcome authoring to shared semantic patterns. Scope: preserve Graduate Outcome ownership, Course-level CILO behavior, and CILO-to-GO mapping rules. Paths: `src/features/outcomes/components/{go-form-dialog,program-head-outcomes-page}.tsx`, `src/app/(app)/program-head/programs/[programId]/outcomes/page.tsx`, and existing outcomes tests. Acceptance: authoring, warning, empty, loading, error, and confirmation states use semantic tokens; role/program scope and outcome ownership are unchanged. Verify: focused outcomes tests, Light/Dark browser checks, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(outcomes): migrate authoring UI`.

## 13. Public, Respondent, Legal, and Error Visual Slices

- [ ] 13.1 Migrate the role selection portal, public login/onboarding/account-status, and public form components to the shared design system. Scope: preserve public entry, Google-authenticated account flow, self-service role claim, and account-status behavior. Paths: `src/features/portals/components/{hero-card,portal-choice-card,portal-shell,role-selection-card,session-banner}.tsx`, `src/features/users/components/{alumni-onboarding-form,faculty-onboarding-form,industry-partner-onboarding-form}.tsx`, `src/app/(public)/{login,onboarding,portal,status}/page.tsx`, `src/app/(public)/onboarding/student-profile-form.tsx`, and existing tests. Acceptance: low-density public pattern, visible labels, helper/error recovery, and responsive role cards intact in both appearances; no public route exposes the showcase, demo switcher, or appearance control. Verify: focused public/onboarding tests, Light/Dark mobile/desktop browser checks, public form accessibility audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(portal): apply semantic public entry UI`.

- [ ] 13.2 Migrate respondent response/review flow presentation to semantic progress, form, warning, dialog, and loading states. Scope: preserve evaluation response submission, guided low-density wizard layout, mobile comfort, and respondent eligibility. Paths: `src/features/responses/components/{review-modal,submitted-response-review,wizard-shell}.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/page.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/[id]/page.tsx`, `src/app/(app)/{student,alumni,industry-partner}/evaluations/[id]/submitted/page.tsx`, and existing tests. Acceptance: progress has text/count support; errors explain recovery; mobile controls meet touch targets. Verify: focused response tests, mobile Light/Dark keyboard/touch browser checks, respondent-flow accessibility audit, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(responses): align respondent flow with semantic tokens`.

- [ ] 13.3 Migrate respondent profile and history data presentation to approved type, status, table, and empty-state rules. Scope: replace raw completed badges and text below the approved minimum. Paths: `src/app/(app)/{student,alumni,industry-partner}/{profile,history}/page.tsx`, and existing tests. Acceptance: history status remains explicitly labeled Completed; profile metadata uses approved type utilities; tables are contained on mobile; bottom-navigation route set is unchanged. Verify: focused respondent profile/history tests, 375px/768px Light/Dark browser checks, scoped text-size/raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(respondents): migrate profile and history UI`.

- [ ] 13.4 Migrate legal, global error, not-found, unauthorized, and route-error presentation to semantic tokens. Scope: replace the raw global-error inline palette; retokenize legal callouts/navigation; preserve legal acknowledgement and safe error recovery. Remove the decorative blur from `PortalShell` that was introduced by `bc07fd9`; preserve the card-count grid behavior. Paths: `src/features/legal/components/{legal-acknowledgement-dialog,legal-document-content,legal-document-nav,legal-footer,legal-page-header,legal-page-shell,legal-section,mobile-legal-document-nav}.tsx`, `src/app/(legal)/privacy/page.tsx`, `src/app/(legal)/terms/page.tsx`, `src/app/{error,global-error,not-found}.tsx`, `src/app/unauthorized/page.tsx`, `src/components/error-boundary.tsx`, `src/components/layout/{operational-route-error,respondent-route-error}.tsx`, `src/features/portals/components/portal-shell.tsx` (decorative blur removal only), and existing tests. Acceptance: legal prose remains readable; error states state the failure and recovery; global error uses semantic Light/Dark-safe presentation; `PortalShell` decorative blur is removed without changing the card-count grid. Verify: focused legal/error tests, Light/Dark browser checks, accessibility audit of error and legal pages, scoped raw-value audit, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(legal): apply unified semantic error and legal UI`.

## 14. Repository Readiness Gate and Operator Activation Runbook

- [ ] 14.1 Complete the cross-repository visual, accessibility, and deployment-boundary readiness gate. This is a committable repository slice, not a deployment action. Scope: remove resolved known-gap rows from `docs/design.md` section 15; refresh the showcase inventory; establish a pass/fail raw-color allowlist for legitimate asset/logo values only; run a minimum `0.75rem` type-scale audit across all migrated surfaces; verify all completed role/page slices; record redacted audit and browser evidence in the issue #225 evidence comment; document the release control in `.env.example` without setting a value that enables it; and document the operator procedure in `docs/runbooks/appearance-production-activation.md`. Paths: `.env.example`, `docs/design.md`, `docs/adr/0010-unified-appearance-and-protected-showcase.md`, `docs/runbooks/appearance-production-activation.md`, `src/features/design-system/data/showcase-fixtures.ts`, and the issue #225 evidence comment. Acceptance: all approved component categories and required states are represented by production sources in the showcase; Light/Dark/System passes at 375px, 768px, 1024px, and 1440px; keyboard focus, contrast, touch target, reduced motion, chart accessibility, loading/empty/error/offline-reference, role-aware navigation, first-paint behavior, and client-chart DTO privacy pass; raw-color audit has no unreviewed component violations; the showcase remains unavailable in primary Production; and no source-code or example-environment default enables appearance rollout. Fail-closed primary Production wording: appearance is enabled only when the server-only `CLOIE_APPEARANCE_ENABLED` release setting is exactly `"true"`; unset, empty, `"false"`, malformed, and all other values remain disabled. Verify: all focused Vitest suites; `pnpm lint`, `pnpm test`, `pnpm build`; Lighthouse accessibility for showcase plus representative public/operational/respondent routes; Chrome DevTools Light/Dark/System visual matrix; effective production CSP check; `pnpm verify:production-auth-boundary`; `pnpm verify:dedicated-demo-auth-boundary`; `pnpm verify:demo-target-isolation`. Commit: `docs(design-system): complete appearance rollout readiness gate`.

- [ ] 14.2 Perform the primary Production appearance activation as an operator action after task 14.1 evidence is accepted. This action is not performed by a Git commit and does not change source code, `.env.example`, or deployment configuration stored in the repository. Scope: follow `docs/runbooks/appearance-production-activation.md` to verify the intended primary Production deployment and project-reference identity, set the server-only deployment environment release setting to exactly `"true"`, verify the selector and OS-Dark parity on the live target, record the required redacted evidence in the issue #225 evidence comment, and use the documented rollback if a regression appears. Paths: deployment configuration and the issue #225 evidence comment only. Acceptance: the operator confirms the target is primary Production before activation; no demo identity or isolated-demo project reference is used as the target; the server-only setting is never exposed through `NEXT_PUBLIC_*`; post-activation checks confirm the appearance selector without product-route changes; and rollback removes or unsets the setting so safe Light tokens remain. Fail-closed primary Production wording: appearance is enabled only when the server-only `CLOIE_APPEARANCE_ENABLED` release setting is exactly `"true"`; unset, empty, `"false"`, malformed, and all other values remain disabled. Verify: execute and record every runbook check in `docs/runbooks/appearance-production-activation.md`, including target identity, pre-activation forced-Light OS-Dark behavior, post-activation selector and OS-Dark parity, and rollback readiness. Commit: none; this is an operator-only deployment action.
