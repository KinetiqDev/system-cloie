## 1. Rebaseline, Ownership, ADR 0010, and CSP Feasibility

- [ ] 1.1 Establish the design-system context, reconcile navigation and analytics ownership with the concurrent `improve-navigation-rendering-and-caching` change, and confirm cross-change contracts. Scope: ADR 0010 already exists on `main` (`docs/adr/0010-unified-appearance-and-protected-showcase.md`, created at `5552b4b` and refined by `dc36974`); create `CONTEXT-MAP.md` entry and `src/features/design-system/CONTEXT.md`; verify that navigation deepest-match, pending, `aria-current`, focus containment, and responsive defect behaviors remain owned by the concurrent rendering change; verify that Faculty analytics DTO privacy narrowing is assigned to `improve-navigation-rendering-and-caching` task 4.5. This task does not edit production code; it establishes the design-system context and confirms ownership boundaries. Paths: `CONTEXT-MAP.md`, new `src/features/design-system/CONTEXT.md`. Acceptance: navigation and analytics DTO ownership are documented with one owning change each; the CONTEXT-MAP.md entry links to ADR 0010 and `src/features/design-system/CONTEXT.md`. Verify: `pnpm lint`, `pnpm build`. Commit: `docs(design-system): establish context and ownership boundaries`.

- [ ] 1.2 Inspect effective deployment Content Security Policy headers and determine the first-paint appearance bootstrap mechanism. Scope: compare four mechanisms against the version-matched Next.js 16.2 CSP documentation — inline bootstrap with `'unsafe-inline'`, same-origin external bootstrap, hash-based SRI (experimental), and nonce-based CSP through `src/proxy.ts`. Document that nonce CSP forces all pages to dynamic rendering, removes static/CDN benefits, and is incompatible with Partial Prerendering. Record the selected mechanism in the durable artifacts; appearance implementation must not proceed until the decision is recorded there. Paths: research-only for code — the task may inspect `src/proxy.ts`, `next.config.ts`, and the effective deployment headers but adds no CSP configuration; durable outputs are `docs/adr/0010-unified-appearance-and-protected-showcase.md` (the Decision statement and section 4 "CSP and first-paint mechanism") and `openspec/changes/migrate-unified-design-system/design.md` (Decision 2 and the Risks/Trade-offs CSP entry). Acceptance: the decision recorded in ADR 0010 and `design.md` selects one bootstrap mechanism and names its exact affected paths, first-paint behavior, CSP requirements, rendering and performance consequences, and rollback; the recorded decision accounts for the existing `src/proxy.ts` session-refresh flow and the streamed `Suspense` root layout; nonce CSP is not a casual fallback. Verify: `pnpm openspec validate migrate-unified-design-system`; record the findings in `docs/adr/0010-unified-appearance-and-protected-showcase.md` (Decision and section 4 "CSP and first-paint mechanism") and in `openspec/changes/migrate-unified-design-system/design.md` (Decision 2 and the Risks/Trade-offs CSP entry); `pnpm lint`, `pnpm build`. Commit: `docs(design-system): record CSP feasibility decision`.

## 2. Semantic Token and Tailwind/shadcn Mapping Parity

- [ ] 2.1 Establish approved Light and Dark semantic token parity in `src/styles/tokens.css` and `src/app/globals.css`. Scope: replace legacy gold secondary family and light-only values with the approved Light and Dark token values from `docs/design.md` sections 5.2–5.5; add the missing `--color-surface-hover`; decouple neutral `accent`, dedicated `ring`, indigo `information`, neutral `secondary`, ACD `brand-accent`, and categorical `chart-*`; add popover, input, overlay, and selection support tokens; map `sidebar-*` to navigation roles; replace the source comment from `docs/design-system.txt` to `docs/design.md`. This task changes token definitions and Tailwind/shadcn mappings only; it does not retokenize components. Paths: `src/styles/tokens.css`, `src/app/globals.css`, and new focused Vitest parity tests at `src/__tests__/components/design-system/semantic-token-parity.test.ts` (Light and Dark token value assertions plus chart-* and sidebar-* mapping assertions) and `src/__tests__/components/design-system/raw-value-audit.test.ts` (scoped raw-color audit over the touched CSS paths). Acceptance: no legacy gold semantic family; no undefined `bg-surface-hover`; no coupled accent/ring/information/chart roles; Light token values match `docs/design.md` section 5.2; Dark token values are present under `.dark` selectors. Verify: `pnpm vitest run src/__tests__/components/design-system/semantic-token-parity.test.ts src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): establish semantic light and dark token foundations`.

## 3. Root Metadata and Public-Shell Semantic Conversion

- [ ] 3.1 Retokenize root metadata, public shell, and manifest color values. Scope: replace `#0051C3` in `src/app/manifest.ts` and `src/app/layout.tsx` viewport `themeColor` with the approved operational primary; retokenize `src/app/(public)/layout.tsx` with semantic classes. This task preserves the current streamed `AuthenticatedAppShell` under `Suspense` and does not reopen the auth shell boundary. Paths: `src/app/layout.tsx` (viewport metadata only), `src/app/manifest.ts`, `src/app/(public)/layout.tsx`, and new focused tests at `src/__tests__/components/design-system/root-metadata-tokens.test.tsx` (asserts no raw `#0051C3` in metadata declarations and that viewport `themeColor` resolves from the approved operational primary), `src/__tests__/app/public-shell-layout.test.tsx` (asserts the public shell uses approved semantic roles and renders correctly in Light and Dark), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched metadata and shell paths). Acceptance: metadata and public shell use approved semantic roles; no raw `#0051C3` remains in metadata declarations; public shell renders correctly in Light and Dark. Verify: `pnpm vitest run src/__tests__/components/design-system/root-metadata-tokens.test.tsx src/__tests__/app/public-shell-layout.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): convert root metadata and public shell to semantic tokens`.

## 4. Existing Shared Primitive Retokenization and Spinner Addition

- [ ] 4.1 Retokenize shared action and loading primitives and add the reviewed Spinner. Scope: retokenize `button.tsx` and `card.tsx` to use semantic token classes; export `CardAction` from `card.tsx`; retain the deprecated `cta-success` branch with semantic success tokens (its removal happens in task 12.1 with its caller); add `spinner.tsx` after reviewing project-aware shadcn source through dry-run; compose the Spinner with Button loading, preserving label/width and preventing duplicate submission. This task depends on 2.1. Paths: `src/components/ui/button.tsx`, `src/components/ui/card.tsx`, `src/components/ui/spinner.tsx`, and new focused tests at `src/__tests__/components/ui/button.test.tsx` (semantic variants including `brand-accent` and Spinner-composed disabled loading that preserves label and width and prevents duplicate activation), `src/__tests__/components/ui/card.test.tsx` (semantic surface plus exported `CardAction` and the temporary `cta-success` success-token resolution), `src/__tests__/components/ui/spinner.test.tsx` (Base UI-only source and no Radix dependency), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched primitive paths). Acceptance: no raw theme value in the listed paths; standard Button variants preserve current APIs and support disabled Spinner-composed loading without width change or duplicate activation; `CardAction` is exported; the temporary `cta-success` resolves semantic success tokens and remains type-safe until task 12.1; `spinner.tsx` is added after project-aware dry-run inspection confirms Base UI source and no Radix dependency. Verify: `pnpm vitest run src/__tests__/components/ui/button.test.tsx src/__tests__/components/ui/card.test.tsx src/__tests__/components/ui/spinner.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): retokenize action primitives and add spinner`.

- [ ] 4.2 Retokenize shared form primitives. Scope: retokenize `checkbox.tsx`, `input.tsx`, `label.tsx`, `radio-group.tsx`, `select.tsx`, `switch.tsx`, `textarea.tsx`, and `field.tsx` to use semantic token classes; verify `field.tsx` composition for visible labels, helper/error text beside the field, and disabled and validation states; checked controls use primary, not semantic success; keep `customZodResolver`. This task depends on 2.1. Paths: `src/components/ui/checkbox.tsx`, `src/components/ui/input.tsx`, `src/components/ui/label.tsx`, `src/components/ui/radio-group.tsx`, `src/components/ui/select.tsx`, `src/components/ui/switch.tsx`, `src/components/ui/textarea.tsx`, `src/components/ui/field.tsx`, and new focused tests at `src/__tests__/components/ui/checkbox.test.tsx`, `src/__tests__/components/ui/input.test.tsx`, `src/__tests__/components/ui/label.test.tsx`, `src/__tests__/components/ui/radio-group.test.tsx`, `src/__tests__/components/ui/select.test.tsx`, `src/__tests__/components/ui/switch.test.tsx`, `src/__tests__/components/ui/textarea.test.tsx`, `src/__tests__/components/ui/field.test.tsx` (visible label, helper, error, valid/invalid, disabled, and checked-uses-primary composition; semantic Light and Dark resolution), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched form primitive paths). Acceptance: no raw theme value in the listed paths; visible labels, helper/error text, valid/invalid/disabled, and checked states resolve semantic tokens in Light and Dark; checked controls use primary. Verify: `pnpm vitest run src/__tests__/components/ui/checkbox.test.tsx src/__tests__/components/ui/input.test.tsx src/__tests__/components/ui/label.test.tsx src/__tests__/components/ui/radio-group.test.tsx src/__tests__/components/ui/select.test.tsx src/__tests__/components/ui/switch.test.tsx src/__tests__/components/ui/textarea.test.tsx src/__tests__/components/ui/field.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): retokenize shared form primitives`.

- [ ] 4.3 Retokenize shared data presentation primitives. Scope: retokenize `table.tsx`, `tabs.tsx`, `progress.tsx`, `skeleton.tsx`, and `empty.tsx` to use semantic token classes; preserve semantic header, hover, selected, and expanded table states; primary marks active tabs and progress; retokenize and verify the existing `empty.tsx`. This task depends on 2.1. Paths: `src/components/ui/table.tsx`, `src/components/ui/tabs.tsx`, `src/components/ui/progress.tsx`, `src/components/ui/skeleton.tsx`, `src/components/ui/empty.tsx`, and new focused tests at `src/__tests__/components/ui/table.test.tsx` (header, hover, selected, and expanded states resolve semantic tokens in Light and Dark), `src/__tests__/components/ui/tabs.test.tsx` (active tab uses primary), `src/__tests__/components/ui/progress.test.tsx` (uses primary), `src/__tests__/components/ui/skeleton.test.tsx`, `src/__tests__/components/ui/empty.test.tsx` (retokenized existing primitive), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched data primitive paths). Acceptance: no raw theme value in the listed paths; table header/hover/selected/expanded, active tab, progress, skeleton, and empty states resolve semantic tokens in Light and Dark without behavior change. Verify: `pnpm vitest run src/__tests__/components/ui/table.test.tsx src/__tests__/components/ui/tabs.test.tsx src/__tests__/components/ui/progress.test.tsx src/__tests__/components/ui/skeleton.test.tsx src/__tests__/components/ui/empty.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): retokenize shared data presentation primitives`.

- [ ] 4.4 Retokenize the sheet overlay surface. Scope: retokenize `sheet.tsx` overlay surface, border, focus, and scrim to use semantic tokens, preserving Base UI title/focus/Escape behavior and the Dialog-to-Drawer responsive substitution. This task depends on 2.1. Paths: `src/components/ui/sheet.tsx`, and new focused tests at `src/__tests__/components/ui/sheet.test.tsx` (overlay surface, border, focus, and scrim resolve semantic tokens; Base UI title/focus/Escape behavior is preserved; Dialog-to-Drawer responsive substitution is preserved) and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched sheet path). Acceptance: no raw theme value in `sheet.tsx`; the overlay surface, border, focus, and scrim resolve semantic tokens; Base UI title/focus/Escape behavior is preserved. Verify: `pnpm vitest run src/__tests__/components/ui/sheet.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): retokenize sheet overlay surface`.

## 5. Appearance Bootstrap, Provider, Storage, and Availability Gate

- [ ] 5.1 Deliver persisted Light, Dark, and System resolution with first-paint correctness after the task 1.2 CSP feasibility decision is recorded in ADR 0010 and `design.md`. Scope: implement the server-only availability resolver, `CLOIE_APPEARANCE_ENABLED` environment contract, the narrow bootstrap script, provider, selector, Settings Appearance route, and avatar-menu entry using the mechanism selected by task 1.2 and recorded in those artifacts. This task depends on tasks 2.1 and 1.2; in `src/app/layout.tsx` it owns only appearance bootstrap/provider insertion. Paths: `.env.example` (release control entry only, empty example value, never `NEXT_PUBLIC_*`), `src/proxy.ts` only if task 1.2 selects nonce CSP — when the recorded decision names inline `'unsafe-inline'`, same-origin external, or hash-based SRI, `src/proxy.ts` is unchanged; when the recorded decision names nonce propagation, the owner of `src/proxy.ts` is this task and the modification is the dedicated appearance nonce propagation path described in `docs/adr/0010-unified-appearance-and-protected-showcase.md`; `src/features/design-system/lib/appearance.ts`; `src/features/design-system/services/resolve-appearance-availability.ts`; `src/features/design-system/components/appearance-bootstrap-script.tsx`; `src/features/design-system/components/appearance-provider.tsx`; `src/features/design-system/components/appearance-selector.tsx`; `src/app/layout.tsx` (appearance insertion only); `src/components/layout/topbar.tsx` (avatar-menu entry only); `src/app/(app)/settings/appearance/page.tsx`; `src/app/(app)/settings/appearance/loading.tsx`; and new focused tests at `src/__tests__/components/design-system/appearance-availability.test.ts` (server-only resolver and `CLOIE_APPEARANCE_ENABLED` parsing), `src/__tests__/components/design-system/appearance-resolution.test.ts` (Light/Dark/System resolution rules and safe parsing), `src/__tests__/components/design-system/appearance-bootstrap-script.test.tsx` (pre-hydration root class application and System `matchMedia` fallback), `src/__tests__/components/design-system/appearance-provider.test.tsx` (post-hydration root class sync and OS listener only while preference is System), `src/__tests__/components/design-system/appearance-selector.test.tsx` (Light/Dark/System radio semantics and visible labels), `src/__tests__/app/settings-appearance-page.test.tsx` (route renders in enabled deployments and returns not-found UI in primary Production without `CLOIE_APPEARANCE_ENABLED`; appearance storage carries no authorization value; enabled appearance changes preserve route/form/filter/scroll/async state), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched appearance paths). Acceptance: in development and valid dedicated demo, missing or malformed preference resolves System, explicit Light/Dark overrides OS, and System responds to OS changes; in primary Production without `CLOIE_APPEARANCE_ENABLED`, the bootstrap forces Light before paint, ignores storage, writes no preference, omits the menu control, and returns not-found UI for Settings Appearance; enabled appearance changes preserve route/form/filter/scroll/async state; appearance storage carries no authorization value. Verify: `pnpm vitest run src/__tests__/components/design-system/appearance-availability.test.ts src/__tests__/components/design-system/appearance-resolution.test.ts src/__tests__/components/design-system/appearance-bootstrap-script.test.tsx src/__tests__/components/design-system/appearance-provider.test.tsx src/__tests__/components/design-system/appearance-selector.test.tsx src/__tests__/app/settings-appearance-page.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the pre-activation OS-Dark browser evidence is recorded as a redacted entry in `docs/runbooks/appearance-production-activation.md` ("Pre-Activation Verification" step) and the issue #225 evidence comment by the operator at task 14.2 time; `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add appearance provider and rollout gate`.

## 6. Protected Showcase Scaffold with Static Fixtures

- [ ] 6.1 Deliver the authenticated, development/dedicated-demo-only Design System Showcase with real production tokens and static fixtures. Scope: create the server access policy, route, loading state, static reference structure, table of contents, foundations inventory, fixture data, and an ordered section registry that later dependent showcase tasks extend without reopening the route page. This task depends on task 2.1 (tokens). Paths: `src/app/(app)/design-system/layout.tsx`, `src/app/(app)/design-system/page.tsx`, `src/app/(app)/design-system/loading.tsx`, `src/features/design-system/services/resolve-showcase-access.ts`, `src/features/design-system/data/showcase-fixtures.ts`, `src/features/design-system/components/design-system-showcase-page.tsx`, `src/features/design-system/components/showcase-section.tsx`, `src/features/design-system/components/token-reference.tsx`, `src/features/design-system/components/component-state-matrix.tsx`, `src/features/design-system/components/showcase-section-registry.tsx`, and new focused tests at `src/__tests__/features/design-system/showcase-access.test.ts` (server access policy: primary Production and invalid demo configuration return not-found UI; unauthenticated and incomplete accounts retain existing guards) and `src/__tests__/app/design-system-page.test.tsx` (route renders inside `AppShell` for authenticated development and valid dedicated demo, returns not-found UI for primary Production and invalid demo, renders scaffold-owned sections from the registry, no role-primary navigation item is added for the showcase, all fixture data is typed, serializable, non-sensitive, and mutation-free, foundations render actual production token classes and primitives). Acceptance: authenticated development and valid dedicated-demo requests render inside the existing `AppShell`; primary Production and invalid demo configuration return not-found UI before content; unauthenticated/incomplete accounts retain existing guards; all fixture data is typed, serializable, non-sensitive, and mutation-free; foundations render actual production token classes and primitives; the registry renders scaffold-owned sections; no role-primary navigation item is added for the showcase. Verify: `pnpm vitest run src/__tests__/features/design-system/showcase-access.test.ts src/__tests__/app/design-system-page.test.tsx`, `pnpm verify:production-auth-boundary`, `pnpm verify:dedicated-demo-auth-boundary`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add protected showcase scaffold`.

## 7. Feedback, Form, Overlay, Empty, Loading, and Offline-Reference Showcase Coverage

- [ ] 7.1 Retokenize feedback, overlay, and form primitives and add their live showcase coverage. Scope: extend the one existing toast contract with information; retokenize `alert.tsx`, `alert-dialog.tsx`, `badge.tsx`, `dialog.tsx`, `drawer.tsx`, `dropdown-menu.tsx`, `toast.tsx`, and `tooltip.tsx`; add interactive form, overlay, popover, toast, table-selection, feedback, and offline-reference showcase sections; add `src/components/ui/popover.tsx` as a required primitive from the approved `base-nova` registry item verified project-aware to generate Base UI-only source with no Radix dependency and no package addition. This task consumes the Button/Spinner API from 4.1; it sequentially extends scaffold-owned `showcase-fixtures.ts` and `showcase-section-registry.ts` from 6.1. Paths: `src/components/ui/alert.tsx`, `src/components/ui/alert-dialog.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/dialog.tsx`, `src/components/ui/drawer.tsx`, `src/components/ui/dropdown-menu.tsx`, `src/components/ui/toast.tsx`, `src/components/ui/tooltip.tsx`, `src/components/ui/popover.tsx`, `src/features/design-system/components/form-controls-showcase.tsx`, `src/features/design-system/components/overlay-and-feedback-showcase.tsx`, `src/features/design-system/components/showcase-section-registry.tsx` (registrations only), `src/features/design-system/data/showcase-fixtures.ts` (additions only), and new focused tests at `src/__tests__/components/ui/alert.test.tsx`, `src/__tests__/components/ui/alert-dialog.test.tsx`, `src/__tests__/components/ui/badge.test.tsx`, `src/__tests__/components/ui/dialog.test.tsx`, `src/__tests__/components/ui/drawer.test.tsx`, `src/__tests__/components/ui/dropdown-menu.test.tsx`, `src/__tests__/components/ui/toast.test.tsx` (information kind added; one root `showToast`/`ToastProvider` event contract preserved), `src/__tests__/components/ui/tooltip.test.tsx`, `src/__tests__/components/ui/popover.test.tsx` (Base UI-only source, imports only `@base-ui/react/popover`, no Radix dependency, no package addition; renders live in the showcase overlay section with Base UI title/focus/Escape behavior), `src/__tests__/components/design-system/form-controls-showcase.test.tsx`, `src/__tests__/components/design-system/overlay-and-feedback-showcase.test.tsx` (forms show visible labels, helper/error text, valid/invalid/disabled state; Button plus Spinner preserve width and prevent duplicate submission while loading; success/warning/danger/information feedback is semantic and readable; overlays preserve Base UI title/focus/Escape behavior; the protected route renders the feedback/overlay section with real interactive controls; showcase examples execute real component behavior with only local fixture state; offline content is explicitly static reference only), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched feedback, overlay, and form primitive paths). Acceptance: forms show visible labels, helper/error text, valid/invalid/disabled state; Button plus Spinner preserve width and prevent duplicate submission while loading; success/warning/danger/information feedback is semantic and readable; overlays preserve Base UI title/focus/Escape behavior; the protected route renders the feedback/overlay section with real interactive controls; showcase examples execute real component behavior with only local fixture state; offline content is explicitly static reference only; `popover.tsx` is added from the approved `base-nova` registry source, imports only `@base-ui/react/popover`, adds no package dependency, and renders live in the showcase overlay section with Base UI title/focus/Escape behavior. Verify: `pnpm vitest run src/__tests__/components/ui/alert.test.tsx src/__tests__/components/ui/alert-dialog.test.tsx src/__tests__/components/ui/badge.test.tsx src/__tests__/components/ui/dialog.test.tsx src/__tests__/components/ui/drawer.test.tsx src/__tests__/components/ui/dropdown-menu.test.tsx src/__tests__/components/ui/toast.test.tsx src/__tests__/components/ui/tooltip.test.tsx src/__tests__/components/ui/popover.test.tsx src/__tests__/components/design-system/form-controls-showcase.test.tsx src/__tests__/components/design-system/overlay-and-feedback-showcase.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the browser keyboard trace and Lighthouse accessibility audit for the showcase are recorded in `docs/reviews/migrate-unified-design-system-task-7-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(design-system): add shared feedback and state showcase coverage`.

## 8. External Prerequisite: Faculty Analytics Aggregate DTO Privacy Invariant

Task 9.1 depends on the Faculty Analytics aggregate DTO privacy invariant completed by `improve-navigation-rendering-and-caching` task 4.5, which owns all implementation and verification: the server computes only word-frequency tokens, response counts, and concise summaries, and the client-facing `FacultyAnalyticsData` type excludes `qualitativeTexts`, raw response rows, respondent identifiers, and account emails. This change does not own any part of that implementation and has no task or commit for it; task 9.1 consumes the completed invariant and its chart-DTO exclusion tests verify the dependency.

## 9. Reviewed Chart Wrapper and Analytics Visualization Migration

- [ ] 9.1 Deliver theme-resolved Recharts presentation and migrate current analytics visualizations after the Faculty Analytics aggregate DTO privacy invariant from `improve-navigation-rendering-and-caching` task 4.5 and tasks 6.1/7.1 showcase and primitive foundations. Scope: add the shadcn-compatible `chart.tsx` wrapper after project-aware dry-run inspection — the dry-run proposes overwriting `card.tsx` and a Recharts dependency entry; neither overwrite nor unaudited dependency change is allowed. Migrate raw color arrays to `--chart-*` tokens; add deterministic non-color differentiation for repeated categories; give every chart a legend, value access, text insight, and tabular or equivalent accessible summary. Sequentially extend the scaffold-owned `showcase-section-registry.ts` with chart registration and add `chart-showcase.tsx`. Paths: `src/components/ui/chart.tsx`, `src/features/analytics/components/course-mean-pie-chart.tsx`, `src/features/analytics/components/faculty-cilo-analytics-chart.tsx`, `src/features/analytics/components/mean-bar-chart.tsx`, `src/features/analytics/components/stakeholder-mean-pie-chart.tsx`, `src/features/analytics/components/qualitative-word-cloud.tsx`, `src/features/analytics/components/faculty-dashboard-visualizations.tsx`, `src/features/analytics/components/faculty-dashboard-visualization-fallbacks.tsx`, `src/features/design-system/components/chart-showcase.tsx`, `src/features/design-system/components/showcase-section-registry.tsx` (chart registration only), and new focused tests at `src/__tests__/components/analytics/course-mean-pie-chart.test.tsx`, `src/__tests__/components/analytics/faculty-cilo-analytics-chart.test.tsx`, `src/__tests__/components/analytics/mean-bar-chart.test.tsx`, `src/__tests__/components/analytics/stakeholder-mean-pie-chart.test.tsx`, `src/__tests__/components/analytics/qualitative-word-cloud.test.tsx`, `src/__tests__/components/analytics/faculty-dashboard-visualizations.test.tsx`, `src/__tests__/components/faculty-dashboard-visualization-fallbacks.test.tsx` (extended to assert `--chart-*` and support tokens, five categorical roles in both appearances, more-than-five category text/marker/pattern distinction, distinct empty/loading/error states, screen-reader chart-insight identification, accessible summary; client chart props exclude raw qualitative text per the task 4.5 privacy invariant; `card.tsx` is not overwritten), `src/__tests__/components/design-system/chart-showcase.test.tsx` (the protected route renders the chart section and its accessible summary), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched chart and analytics paths). Acceptance: all listed charts consume `--chart-*` and support tokens; five categorical roles resolve in both appearances; more-than-five category rendering has text/marker/pattern distinction; empty/loading/error states are distinct; screen readers can identify the chart insight and obtain displayed values; client chart props exclude raw qualitative text per the task 4.5 privacy invariant; the protected route renders the chart section and its accessible summary; `card.tsx` is not overwritten. Verify: `pnpm vitest run src/__tests__/components/analytics/course-mean-pie-chart.test.tsx src/__tests__/components/analytics/faculty-cilo-analytics-chart.test.tsx src/__tests__/components/analytics/mean-bar-chart.test.tsx src/__tests__/components/analytics/stakeholder-mean-pie-chart.test.tsx src/__tests__/components/analytics/qualitative-word-cloud.test.tsx src/__tests__/components/analytics/faculty-dashboard-visualizations.test.tsx src/__tests__/components/faculty-dashboard-visualization-fallbacks.test.tsx src/__tests__/components/design-system/chart-showcase.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the browser desktop/mobile Light/Dark screenshots are recorded in `docs/reviews/migrate-unified-design-system-task-9-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `feat(analytics): migrate charts to semantic palettes`.

## 10. Navigation Visual Retokenization Only

- [ ] 10.1 Retokenize role-aware navigation surfaces without changing navigation information architecture or matching behavior. Scope: repair semantic hover/active/focus styles in desktop sidebar, Dean tablet rail, mobile drawer, and respondent bottom navigation. This task consumes current `getDeepestMatchingNavItem` matching, pending behavior, `aria-current`, and responsive navigation modes from the concurrent rendering change without reopening them. Add `navigation-showcase.tsx` and `responsive-showcase.tsx` entries to the showcase registry. Paths: `src/components/layout/app-shell.tsx`, `src/components/layout/sidebar.tsx`, `src/components/layout/mobile-sidebar-drawer.tsx`, `src/components/layout/mobile-nav.tsx`, `src/components/layout/navigation-link.tsx`, `src/lib/constants/navigation.ts` (visual-only, no route changes), `src/features/design-system/components/navigation-showcase.tsx`, `src/features/design-system/components/responsive-showcase.tsx`, `src/features/design-system/components/showcase-section-registry.tsx` (registrations only), and new focused tests at `src/__tests__/components/sidebar.test.tsx` (extended to assert semantic hover, active, and focus in Light and Dark across role and viewport boundaries), `src/__tests__/components/mobile-nav-boundary.test.ts` (extended to assert touch targets and visible focus for mobile drawer and bottom navigation), `src/__tests__/components/mobile-sidebar-drawer.test.tsx` (extended to assert semantic hover, active, and focus in mobile drawer and Dean tablet rail), `src/__tests__/components/navigation-link.test.tsx` (extended to assert semantic selected/primary active state, visible focus, and logo plate in Dark mode), `src/__tests__/components/design-system/navigation-showcase.test.tsx` (renders real navigation declarations and layout seams; no role-primary item is added), `src/__tests__/components/design-system/responsive-showcase.test.tsx` (tablet/mobile substitutions retain current role rules), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched navigation paths). Acceptance: active navigation uses selected/primary tokens; hover resolves through defined surface roles; all interactive navigation retains visible focus; tablet/mobile substitutions retain current role rules; no role-primary item is added for the showcase; `navigation-showcase.tsx` renders real navigation declarations and layout seams; logo treatment uses the approved light brand-safe plate in Dark mode. Verify: `pnpm vitest run src/__tests__/components/sidebar.test.tsx src/__tests__/components/mobile-nav-boundary.test.ts src/__tests__/components/mobile-sidebar-drawer.test.tsx src/__tests__/components/navigation-link.test.tsx src/__tests__/components/design-system/navigation-showcase.test.tsx src/__tests__/components/design-system/responsive-showcase.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the keyboard/tap-target browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-10-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(navigation): apply semantic navigation roles`.

## 11. Administrative Domain Visual Slices

- [ ] 11.1 Migrate Secretary user-management data display and edit dialogs to semantic status, form, table, and confirmation patterns. Scope: remove the three duplicated role-color helpers and raw success values while preserving account creation, protected account edit, active/inactive state, and server authorization behavior. Paths: `src/features/users/components/stat-cards.tsx`, `src/features/users/components/secretary-add-user-form.tsx`, `src/features/users/components/secretary-users-list/index.tsx`, `src/features/users/components/secretary-users-list/users-data-table.tsx`, `src/features/users/components/secretary-users-list/user-dialogs.tsx`, `src/features/users/components/secretary-users-list/edit-user-dialog.tsx`, `src/features/users/components/secretary-users-list/users-filter-bar.tsx`, `src/features/users/components/secretary-users-list/users-pagination.tsx`, `src/features/users/components/secretary-users-list/users-kpi.tsx`, `src/app/(app)/secretary/users/page.tsx`, and the focused tests `src/__tests__/components/secretary/secretary-users-list.test.tsx` and `src/__tests__/components/secretary/secretary-add-user-form.test.tsx` (extended to assert semantic role/category badges, lifecycle feedback, tables, labels, and dialogs in Light and Dark, exact protected-account review and confirmation behavior, and no raw palette class) plus `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched Secretary user-management paths). Acceptance: role/category badges, lifecycle feedback, tables, labels, and dialogs resolve semantic roles in Light/Dark; protected account edits retain exact review/confirmation behavior; no raw palette class remains. Verify: `pnpm vitest run src/__tests__/components/secretary/secretary-users-list.test.tsx src/__tests__/components/secretary/secretary-add-user-form.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the desktop/mobile Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-11-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(users): apply semantic management UI`.

- [ ] 11.2 Migrate Academic Calendar management surfaces to shared semantic field, badge, dialog, loading, and error patterns. Scope: visual-only conversion; preserve school year, semester, academic term, Summer semester, assignment period, and active academic period behavior. Paths: `src/features/academic-calendar/components/active-term-badge.tsx`, `src/features/academic-calendar/components/rollover-exceptions-table.tsx`, `src/features/academic-calendar/components/rollover-runner.tsx`, `src/features/academic-calendar/components/school-year-form.tsx`, `src/features/academic-calendar/components/school-year-list.tsx`, `src/features/academic-calendar/components/set-active-term-dialog.tsx`, `src/features/academic-calendar/components/term-instance-form.tsx`, `src/features/academic-calendar/components/term-instance-picker.tsx`, `src/app/(app)/secretary/school-years/page.tsx`, `src/app/(app)/secretary/school-years/[id]/client-page.tsx`, and the focused tests `src/__tests__/components/term-instance-picker.test.tsx` (extended to assert semantic lifecycle, status, empty, loading, and error states in Light and Dark) and `src/__tests__/components/admin/rollover-runner.test.tsx` (extended to assert semantic lifecycle, loading, and error states) plus `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched Academic Calendar paths). Acceptance: lifecycle/status/empty/loading/error states are semantic and accessible in both appearances. Verify: `pnpm vitest run src/__tests__/components/term-instance-picker.test.tsx src/__tests__/components/admin/rollover-runner.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-11-2.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-calendar): align management UI with tokens`.

- [ ] 11.3 Migrate Academic Structure program/course management surfaces to semantic cards, badges, forms, dialogs, and tables. Scope: preserve program lifecycle steward authority, program deactivation, program deletion, major management, and Course catalog behavior. Paths: `src/features/academic-structure/components/secretary-programs-list.tsx`, `src/features/academic-structure/components/program-form.tsx`, `src/features/academic-structure/components/management-courses-list.tsx`, `src/features/academic-structure/components/course-form.tsx`, `src/features/academic-structure/components/manage-majors-dialog.tsx`, `src/features/academic-structure/components/program-head-courses-catalog.tsx`, `src/app/(app)/secretary/programs/page.tsx`, `src/app/(app)/secretary/courses/page.tsx`, `src/app/(app)/program-head/programs/[programId]/courses/page.tsx`, `src/app/(app)/dean/academic-structure/programs/page.tsx`, `src/app/(app)/dean/academic-structure/courses/page.tsx`, and the focused tests `src/__tests__/components/secretary/secretary-programs-list.test.tsx` (extended to assert semantic category representations), `src/__tests__/components/academic-structure/course-form-temporal-fields.test.tsx` (extended to assert semantic form, dialog, and table state), `src/__tests__/components/academic-structure/program-head-courses-catalog.test.tsx` (extended to assert canonical `programs/[programId]` Program Head route and semantic catalog state), and `src/__tests__/components/management-courses-list.test.tsx` (extended to assert semantic course management state) plus `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched Academic Structure paths). Acceptance: category representations use approved semantic roles; Program Head route uses canonical `programs/[programId]` path. Verify: `pnpm vitest run src/__tests__/components/secretary/secretary-programs-list.test.tsx src/__tests__/components/academic-structure/course-form-temporal-fields.test.tsx src/__tests__/components/academic-structure/program-head-courses-catalog.test.tsx src/__tests__/components/management-courses-list.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-11-3.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(academic-structure): migrate management UI to semantic tokens`.

- [ ] 11.4 Migrate Course Assignment and enrollment management displays to shared semantic tables, alerts, filters, status, and confirmation patterns. Scope: preserve Course Assignment, membership, roster eligibility, role-owned route, and all-program Course assignment manager invariants. Paths: `src/features/course-assignments/components/course-assignment-form-dialog.tsx`, `src/features/course-assignments/components/course-assignments-page-shell.tsx`, `src/features/course-assignments/components/course-assignments-table.tsx`, `src/features/course-assignments/components/course-roster-management.tsx`, `src/features/course-assignments/components/course-roster-pages.tsx`, `src/features/course-assignments/components/course-row-assignments-sheet.tsx`, `src/features/course-assignments/components/edit-course-assignment-dialog.tsx`, `src/features/course-assignments/components/shared/assignment-filters.tsx`, `src/features/course-assignments/components/shared/assignment-summary-block.tsx`, `src/features/course-assignments/components/shared/class-identity-fields.tsx`, `src/features/course-assignments/components/shared/faculty-search-popover.tsx`, `src/features/course-assignments/components/shared/wizard-stepper.tsx`, `src/features/enrollments/components/enrollment-editor-dialog.tsx`, `src/features/enrollments/components/student-enrollment-history.tsx`, `src/app/(app)/secretary/course-assignments/page.tsx`, `src/app/(app)/dean/academic-structure/course-assignments/page.tsx`, `src/app/(app)/program-head/programs/[programId]/course-assignments/page.tsx`, `src/app/(app)/faculty/course-rosters/page.tsx`, and the focused tests `src/__tests__/components/course-assignments/course-assignment-form-dialog.test.tsx` (extended to assert semantic form, table, alert, filter, status, and confirmation states; Program Head route uses canonical `programs/[programId]` path; wide tables stay contained), `src/__tests__/components/course-assignments/course-assignments-table.test.tsx` (extended to assert semantic table state, contained layout, and safe domain wording), `src/__tests__/components/course-assignments/course-assignments-page-shell.test.tsx` (extended to assert semantic shell state), `src/__tests__/components/course-assignments/course-roster-pages.test.tsx` (extended to assert semantic roster state), `src/__tests__/components/course-assignments/edit-course-assignment-dialog.test.tsx` (extended to assert semantic edit dialog state), and `src/__tests__/components/assignment-filters.test.tsx` (extended to assert semantic filter state) plus `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched course-assignment and enrollment paths). Acceptance: lifecycle messages preserve safe domain wording; wide tables stay contained; Program Head route uses canonical `programs/[programId]` path. Verify: `pnpm vitest run src/__tests__/components/course-assignments/course-assignment-form-dialog.test.tsx src/__tests__/components/course-assignments/course-assignments-table.test.tsx src/__tests__/components/course-assignments/course-assignments-page-shell.test.tsx src/__tests__/components/course-assignments/course-roster-pages.test.tsx src/__tests__/components/course-assignments/edit-course-assignment-dialog.test.tsx src/__tests__/components/assignment-filters.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-11-4.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(course-assignments): apply semantic operational states`.

## 12. Authoring and Evaluation Domain Visual Slices

- [ ] 12.1 Migrate instrument and template authoring UI to semantic action, field, feedback, and loading patterns. Scope: replace raw danger/success and hardcoded action treatments; remove the deprecated `cta-success` branch from `button.tsx` in the same commit as its only `template-builder.tsx` caller moves to the approved action hierarchy. Paths: `src/features/instruments/components/faculty-tools-page.tsx`, `src/features/instruments/components/management-template-builder.tsx`, `src/features/instruments/components/management-tools-page.tsx`, `src/features/instruments/components/program-head-template-builder.tsx`, `src/features/instruments/components/program-head-tools-page.tsx`, `src/features/instruments/components/template-builder.tsx`, `src/features/instruments/components/templates-grid.tsx`, `src/components/ui/button.tsx` (`cta-success` branch removal only), `src/app/(app)/secretary/instruments/page.tsx`, `src/app/(app)/faculty/tools/page.tsx`, `src/app/(app)/program-head/programs/[programId]/tools/page.tsx`, and the focused tests `src/__tests__/components/instruments/management-template-builder.test.tsx` (extended to assert semantic action, field, feedback, and loading states; destructive controls retain confirmation and recovery semantics), `src/__tests__/components/instruments/management-tools-page.test.tsx` (extended to assert semantic management tools state), `src/__tests__/components/instruments/template-builder.test.tsx` (extended to assert the former `cta-success` caller uses an approved replacement and the route is on the canonical `programs/[programId]` path), `src/__tests__/components/ui/button.test.tsx` (extended to assert `cta-success` is removed and `brand-accent` and standard variants are semantic), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched instruments paths). Acceptance: routine secondary actions remain neutral; specialized `brand-accent` actions are separately named; the former `cta-success` caller uses an approved replacement; destructive controls retain confirmation/recovery semantics; `cta-success` is removed; Program Head routes use canonical `programs/[programId]` paths. Verify: `pnpm vitest run src/__tests__/components/instruments/management-template-builder.test.tsx src/__tests__/components/instruments/management-tools-page.test.tsx src/__tests__/components/instruments/template-builder.test.tsx src/__tests__/components/ui/button.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the keyboard/reduced-motion Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-12-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(instruments): align authoring UI with semantic tokens`.

- [ ] 12.2 Migrate Course-bound evaluation deployment and review controls to semantic lifecycle, validation, confirmation, and status treatment. Scope: preserve evaluation publication, recipient exclusion, roster lock, eligibility, and close behavior. Paths: `src/features/evaluations/components/assignment-picker.tsx`, `src/features/evaluations/components/close-evaluation-dialog.tsx`, `src/features/evaluations/components/evaluation-detail-dialog.tsx`, `src/features/evaluations/components/faculty-cilos-course-list.tsx`, `src/features/evaluations/components/faculty-published-evaluations-table.tsx`, `src/features/evaluations/components/index.tsx`, `src/features/evaluations/components/late-include-dialog.tsx`, `src/features/evaluations/components/publish-central-deployment-form.tsx`, `src/features/evaluations/components/publish-course-bound-evaluation-form-v2.tsx`, `src/app/(app)/faculty/cilo-evaluations/new/page.tsx`, `src/app/(app)/faculty/cilo-evaluations/[evaluationId]/page.tsx`, and the focused tests `src/__tests__/components/faculty/late-include-dialog.test.tsx` (extended to assert semantic lifecycle, validation, confirmation, and status states; confirmed destructive actions retain safe cancellation), `src/__tests__/components/faculty/publish-course-bound-evaluation-form.test.tsx` (extended to assert semantic deployment state and validation), `src/__tests__/components/course-bound-review/course-bound-review-tabs.test.tsx` (extended to assert semantic review state), `src/__tests__/components/course-bound-review/anonymized-response-detail.test.tsx` (extended to assert semantic response detail state), `src/__tests__/components/course-bound-review/qualitative-word-cloud.test.tsx` (extended to assert semantic word cloud state), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched evaluations paths). Acceptance: validation and lifecycle notices state cause and recovery; confirmed destructive actions retain safe cancellation. Verify: `pnpm vitest run src/__tests__/components/faculty/late-include-dialog.test.tsx src/__tests__/components/faculty/publish-course-bound-evaluation-form.test.tsx src/__tests__/components/course-bound-review/course-bound-review-tabs.test.tsx src/__tests__/components/course-bound-review/anonymized-response-detail.test.tsx src/__tests__/components/course-bound-review/qualitative-word-cloud.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the keyboard/overlay Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-12-2.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(evaluations): apply semantic deployment states`.

- [ ] 12.3 Migrate Program Head outcome authoring to shared semantic patterns. Scope: preserve Graduate Outcome ownership, Course-level CILO behavior, and CILO-to-GO mapping rules. Paths: `src/features/outcomes/components/go-form-dialog.tsx`, `src/features/outcomes/components/program-head-outcomes-page.tsx`, `src/app/(app)/program-head/programs/[programId]/outcomes/page.tsx`, and the focused tests `src/__tests__/features/outcomes/manage-cilo-mappings.test.ts` (extended to assert semantic authoring, warning, empty, loading, error, and confirmation states and unchanged CILO-to-GO mapping rules) and `src/__tests__/features/outcomes/manage-outcome-writes.test.ts` (extended to assert unchanged role/program scope and outcome ownership and semantic dialog state) plus `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched outcomes paths). Acceptance: authoring, warning, empty, loading, error, and confirmation states use semantic tokens; role/program scope and outcome ownership are unchanged. Verify: `pnpm vitest run src/__tests__/features/outcomes/manage-cilo-mappings.test.ts src/__tests__/features/outcomes/manage-outcome-writes.test.ts src/__tests__/components/design-system/raw-value-audit.test.ts`; the Light/Dark browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-12-3.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(outcomes): migrate authoring UI`.

## 13. Public, Respondent, Legal, and Error Visual Slices

- [ ] 13.1 Migrate the role selection portal, public login/onboarding/account-status, and public form components to the shared design system. Scope: preserve public entry, Google-authenticated account flow, self-service role claim, and account-status behavior; remove the decorative blur from `PortalShell` that was introduced by `bc07fd9`; preserve the card-count grid behavior. Paths: `src/features/portals/components/hero-card.tsx`, `src/features/portals/components/portal-choice-card.tsx`, `src/features/portals/components/portal-shell.tsx`, `src/features/portals/components/role-selection-card.tsx`, `src/features/portals/components/session-banner.tsx`, `src/features/users/components/alumni-onboarding-form.tsx`, `src/features/users/components/faculty-onboarding-form.tsx`, `src/features/users/components/industry-partner-onboarding-form.tsx`, `src/app/(public)/login/page.tsx`, `src/app/(public)/onboarding/page.tsx`, `src/app/(public)/portal/page.tsx`, `src/app/(public)/status/page.tsx`, `src/app/(public)/onboarding/student-profile-form.tsx`, and the focused tests `src/__tests__/features/portals/portal-shell-layout.test.tsx` (extended to assert decorative blur is removed and three-card centered versus four-card grid behavior is preserved), `src/__tests__/features/portals/portal-login-flow.test.tsx` (extended to assert low-density public pattern, visible labels, helper and error recovery, and responsive role cards in both appearances), `src/__tests__/features/portals/portal-onboarding-flow.test.ts` (extended to assert Google-authenticated account flow, self-service role claim, and account-status behavior in both appearances), `src/__tests__/features/portals/role-card-config.test.ts` (extended to assert responsive role card grid), `src/__tests__/app/student-profile-form.test.tsx` (extended to assert the public onboarding student profile form uses semantic tokens in both appearances), and `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched public and onboarding paths). Acceptance: `PortalShell` decorative blur is removed; the three-card centered grid versus four-card grid behavior from `bc07fd9` is preserved; low-density public pattern, visible labels, helper/error recovery, and responsive role cards intact in both appearances; no public route exposes the showcase, demo switcher, or appearance control. Verify: `pnpm vitest run src/__tests__/features/portals/portal-shell-layout.test.tsx src/__tests__/features/portals/portal-login-flow.test.tsx src/__tests__/features/portals/portal-onboarding-flow.test.ts src/__tests__/features/portals/role-card-config.test.ts src/__tests__/app/student-profile-form.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the public form accessibility audit and Light/Dark mobile/desktop browser checks are recorded in `docs/reviews/migrate-unified-design-system-task-13-1.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(portal): apply semantic public entry UI`.

- [ ] 13.2 Migrate respondent response/review flow presentation to semantic progress, form, warning, dialog, and loading states. Scope: preserve evaluation response submission, guided low-density wizard layout, mobile comfort, and respondent eligibility. Paths: `src/features/responses/components/review-modal.tsx`, `src/features/responses/components/submitted-response-review.tsx`, `src/features/responses/components/wizard-shell.tsx`, `src/app/(app)/student/evaluations/page.tsx`, `src/app/(app)/alumni/evaluations/page.tsx`, `src/app/(app)/industry-partner/evaluations/page.tsx`, `src/app/(app)/student/evaluations/[id]/page.tsx`, `src/app/(app)/alumni/evaluations/[id]/page.tsx`, `src/app/(app)/industry-partner/evaluations/[id]/page.tsx`, `src/app/(app)/student/evaluations/[id]/submitted/page.tsx`, `src/app/(app)/alumni/evaluations/[id]/submitted/page.tsx`, `src/app/(app)/industry-partner/evaluations/[id]/submitted/page.tsx`, and the focused test `src/__tests__/components/student/evaluations/wizard-shell.test.tsx` (extended to assert semantic progress with text and count support, errors explain recovery, mobile controls meet touch targets, and evaluation response submission, guided low-density wizard layout, mobile comfort, and respondent eligibility are preserved) plus new focused tests at `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched response flow paths). Acceptance: progress has text/count support; errors explain recovery; mobile controls meet touch targets. Verify: `pnpm vitest run src/__tests__/components/student/evaluations/wizard-shell.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the mobile Light/Dark keyboard/touch browser checks and respondent-flow accessibility audit are recorded in `docs/reviews/migrate-unified-design-system-task-13-2.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(responses): align respondent flow with semantic tokens`.

- [ ] 13.3 Migrate respondent profile and history data presentation to approved type, status, table, and empty-state rules. Scope: replace raw completed badges and text below the approved minimum. Paths: `src/app/(app)/student/profile/page.tsx`, `src/app/(app)/alumni/profile/page.tsx`, `src/app/(app)/industry-partner/profile/page.tsx`, `src/app/(app)/student/history/page.tsx`, `src/app/(app)/alumni/history/page.tsx`, `src/app/(app)/industry-partner/history/page.tsx`, and the focused tests `src/__tests__/app/student-profile-form.test.tsx` (extended to assert approved type utilities on profile metadata; already covers the public student profile form path; the respondent profile page at `src/app/(app)/student/profile/page.tsx` is a separate page that requires a new focused test `src/__tests__/app/student-profile-page.test.tsx` for semantic profile state including contained tables on mobile, bottom-navigation route set unchanged, and no new role-primary navigation item) and `src/__tests__/app/student-evaluation-pages.test.tsx` (extended to assert history status remains explicitly labeled Completed; contained tables on mobile; bottom-navigation route set unchanged) plus new focused tests at `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched respondent profile and history paths). Acceptance: history status remains explicitly labeled Completed; profile metadata uses approved type utilities; tables are contained on mobile; bottom-navigation route set is unchanged. Verify: `pnpm vitest run src/__tests__/app/student-profile-form.test.tsx src/__tests__/app/student-profile-page.test.tsx src/__tests__/app/student-evaluation-pages.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the 375px/768px Light/Dark browser checks and scoped text-size audit are recorded in `docs/reviews/migrate-unified-design-system-task-13-3.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(respondents): migrate profile and history UI`.

- [ ] 13.4 Migrate legal, global error, not-found, unauthorized, and route-error presentation to semantic tokens. Scope: replace the raw global-error inline palette; retokenize legal callouts/navigation; preserve legal acknowledgement and safe error recovery. Paths: `src/features/legal/components/legal-acknowledgement-dialog.tsx`, `src/features/legal/components/legal-document-content.tsx`, `src/features/legal/components/legal-document-nav.tsx`, `src/features/legal/components/legal-footer.tsx`, `src/features/legal/components/legal-page-header.tsx`, `src/features/legal/components/legal-page-shell.tsx`, `src/features/legal/components/legal-section.tsx`, `src/features/legal/components/mobile-legal-document-nav.tsx`, `src/app/(legal)/privacy/page.tsx`, `src/app/(legal)/terms/page.tsx`, `src/app/error.tsx`, `src/app/global-error.tsx`, `src/app/not-found.tsx`, `src/app/unauthorized/page.tsx`, `src/components/error-boundary.tsx`, `src/components/layout/operational-route-error.tsx`, `src/components/layout/respondent-route-error.tsx`, and the focused tests `src/__tests__/features/legal/legal-acknowledgement-dialog.test.tsx` (extended to assert semantic legal acknowledgement, legal prose remains readable, and safe error recovery), `src/__tests__/features/legal/legal-content.test.ts` (extended to assert semantic legal callouts and navigation), `src/__tests__/features/legal/legal-acknowledgement-ticket.test.ts` (extended to assert semantic legal acknowledgement flow), `src/__tests__/app/respondent-route-error.test.tsx` (extended to assert semantic respondent route error state), `src/__tests__/app/operational-route-rendering.test.tsx` (extended to assert semantic operational route error state), `src/__tests__/app/privacy-page.test.tsx` (extended to assert semantic privacy page state), `src/__tests__/app/terms-page.test.tsx` (extended to assert semantic terms page state), `src/__tests__/app/status-pages.test.tsx` (extended to assert semantic not-found and unauthorized state), and `src/__tests__/components/authenticated-shell-fallback.test.tsx` (extended to assert semantic authenticated shell fallback state) plus new focused tests at `src/__tests__/components/design-system/raw-value-audit.test.ts` (extended to cover the touched legal and error paths). Acceptance: legal prose remains readable; error states state the failure and recovery; global error uses semantic Light/Dark-safe presentation. Verify: `pnpm vitest run src/__tests__/features/legal/legal-acknowledgement-dialog.test.tsx src/__tests__/features/legal/legal-content.test.ts src/__tests__/features/legal/legal-acknowledgement-ticket.test.ts src/__tests__/app/respondent-route-error.test.tsx src/__tests__/app/operational-route-rendering.test.tsx src/__tests__/app/privacy-page.test.tsx src/__tests__/app/terms-page.test.tsx src/__tests__/app/status-pages.test.tsx src/__tests__/components/authenticated-shell-fallback.test.tsx src/__tests__/components/design-system/raw-value-audit.test.ts`; the Light/Dark browser checks and accessibility audit of error and legal pages are recorded in `docs/reviews/migrate-unified-design-system-task-13-4.md` (created by this task); `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `refactor(legal): apply unified semantic error and legal UI`.

## 14. Repository Readiness Gate and Operator Activation Runbook

- [ ] 14.1 Complete the cross-repository visual, accessibility, and deployment-boundary readiness gate. This is a committable repository slice, not a deployment action. Scope: remove resolved known-gap rows from `docs/design.md` section 15; refresh the showcase inventory; establish a pass/fail raw-color allowlist for legitimate asset/logo values only; run a minimum `0.75rem` type-scale audit across all migrated surfaces; verify all completed role/page slices; record redacted audit and browser evidence in the issue #225 evidence comment; document the release control in `.env.example` without setting a value that enables it; and document the operator procedure in `docs/runbooks/appearance-production-activation.md`. Paths: `.env.example`, `docs/design.md`, `docs/adr/0010-unified-appearance-and-protected-showcase.md`, `docs/runbooks/appearance-production-activation.md`, `src/features/design-system/data/showcase-fixtures.ts`, and the issue #225 evidence comment. Acceptance: all approved component categories and required states are represented by production sources in the showcase; Light/Dark/System passes at 375px, 768px, 1024px, and 1440px; keyboard focus, contrast, touch target, reduced motion, chart accessibility, loading/empty/error/offline-reference, role-aware navigation, first-paint behavior, and client-chart DTO privacy pass; raw-color audit has no unreviewed component violations; the showcase remains unavailable in primary Production; and no source-code or example-environment default enables appearance rollout. Fail-closed primary Production wording: appearance is enabled only when the server-only `CLOIE_APPEARANCE_ENABLED` release setting is exactly `"true"`; unset, empty, `"false"`, malformed, and all other values remain disabled. Verify: all focused Vitest suites; `pnpm lint`, `pnpm test`, `pnpm build`; Lighthouse accessibility for showcase plus representative public/operational/respondent routes; Chrome DevTools Light/Dark/System visual matrix; effective production CSP check; `pnpm verify:production-auth-boundary`; `pnpm verify:dedicated-demo-auth-boundary`; `pnpm verify:demo-target-isolation`. Commit: `docs(design-system): complete appearance rollout readiness gate`.

- [ ] 14.2 Perform the primary Production appearance activation as an operator action after task 14.1 evidence is accepted. This action is not performed by a Git commit and does not change source code, `.env.example`, or deployment configuration stored in the repository. Scope: follow `docs/runbooks/appearance-production-activation.md` to verify the intended primary Production deployment and project-reference identity, set the server-only deployment environment release setting to exactly `"true"`, verify the selector and OS-Dark parity on the live target, record the required redacted evidence in the issue #225 evidence comment, and use the documented rollback if a regression appears. Paths: `docs/runbooks/appearance-production-activation.md` (the durable operator procedure and runbook check sequence that governs this action; the operator records the required redacted evidence in the issue #225 evidence comment and updates the same runbook's "Evidence Limits" section by following the existing record), the primary Production hosting environment's `CLOIE_APPEARANCE_ENABLED` release setting (server-only, never `NEXT_PUBLIC_*`), and the issue #225 evidence comment. No repository source files are modified by this task. Acceptance: the operator confirms the target is primary Production before activation; no demo identity or isolated-demo project reference is used as the target; the server-only setting is never exposed through `NEXT_PUBLIC_*`; post-activation checks confirm the appearance selector without product-route changes; and rollback removes or unsets the setting so safe Light tokens remain. Fail-closed primary Production wording: appearance is enabled only when the server-only `CLOIE_APPEARANCE_ENABLED` release setting is exactly `"true"`; unset, empty, `"false"`, malformed, and all other values remain disabled. Verify: execute and record every runbook check in `docs/runbooks/appearance-production-activation.md`, including target identity, pre-activation forced-Light OS-Dark behavior, post-activation selector and OS-Dark parity, and rollback readiness. Commit: none; this is an operator-only deployment action.
