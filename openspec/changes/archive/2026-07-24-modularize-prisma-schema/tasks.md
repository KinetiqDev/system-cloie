## 1. Compatibility Gate

- [x] 1.1 Upgrade `prisma` and `@prisma/client` to `6.19.2`, verify compatibility, inspect release notes, and update only `package.json` plus `pnpm-lock.yaml`; acceptance: `pnpm exec prisma -v` reports matching `6.19.2` versions and no Prisma 7 configuration is introduced; verify with `pnpm exec prisma -v`; commit: `build(prisma): enable stable schema folders`
- [x] 1.2 Establish temporary before-state evidence from `prisma/schema.prisma`: formatted schema, generated Client success, from-empty SQL diff, and model/enum/relation inventory; acceptance: evidence is stored outside repository or in ignored temp output and current worktree remains free of database changes; verify with `pnpm exec prisma format --schema prisma/schema.prisma`, `pnpm exec prisma validate --schema prisma/schema.prisma`, `pnpm exec prisma generate --schema prisma/schema.prisma`, and `pnpm exec prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script`; commit: `test(prisma): capture schema refactor baseline`

## 2. Domain Schema Slices

- [x] 2.1 Reduce `prisma/schema.prisma` to existing generator and datasource blocks, add `prisma/models/academic-calendar.prisma` and `prisma/models/academic-structure.prisma`, and move `AcademicSemester`, `AcademicTerm`, `AcademicPeriodStatus`, `SchoolYear`, `AcademicTermInstance`, `AcademicPeriodReadinessSnapshot`, `Program`, and `Major` unchanged; acceptance: calendar and structure definitions validate with exact maps, indexes, composite key, and referential actions; verify with `pnpm exec prisma format --schema prisma`, `pnpm exec prisma validate --schema prisma`; commit: `refactor(prisma): split calendar and structure models`
- [x] 2.2 Add `prisma/models/identity-access.prisma` and move `SystemRole`, `InviteStatus`, `VerificationStatus`, `User`, `UserRole`, `StudentAcademicProfile`, `IndustryPartnerProfile`, `AlumniProfile`, `ExternalStakeholderInvite`, `FacultyProgramAffiliation`, and `ProgramHeadAssignment` unchanged; acceptance: single-role, auth-link, profile, composite alumni relation, and named relation invariants remain present; verify with `pnpm exec prisma validate --schema prisma` and focused auth/user tests; commit: `refactor(prisma): split identity and access models`
- [x] 2.3 Add `prisma/models/course-assignments.prisma` and move `YearLevel`, `CourseScope`, `StudentSection`, `EnrollmentSource`, `Course`, `StudentEnrollment`, `CourseAssignment`, and `CourseAssignmentMembership` unchanged; acceptance: course catalog defaults, enrollment key, membership composite FK target, named indexes, `onUpdate: Restrict`, and SQL-only constraint mirrors remain unchanged; verify with `pnpm exec prisma validate --schema prisma` and `pnpm vitest run src/__tests__/features/course-assignments src/__tests__/modules/course-assignments`; commit: `refactor(prisma): split course assignment models`
- [x] 2.4 Add `prisma/models/outcomes.prisma` and move `GO`, `CILO`, and `CILOMapping` unchanged; acceptance: program-owned GO, course-level CILO, mapping uniqueness, author relation, and archive field remain unchanged; verify with `pnpm exec prisma validate --schema prisma` and focused outcomes tests; commit: `refactor(prisma): split outcomes models`
- [x] 2.5 Add `prisma/models/instruments.prisma` and move `EvaluationTemplateType`, `InstrumentTemplate`, `InstrumentVersion`, and `InstrumentTemplateCiloQuestionBinding` unchanged; acceptance: template copy relation and course/program/major binding relations retain exact names, mappings, and actions; verify with `pnpm exec prisma validate --schema prisma` and focused instrument tests; commit: `refactor(prisma): split instrument models`
- [x] 2.6 Add `prisma/models/evaluations-deployments.prisma` and move `DeploymentStatus`, `TargetStakeholder`, `CourseBoundEvaluationExclusionCategory`, `CourseBoundEvaluationExclusionReversalCategory`, `CourseBoundEvaluation`, `CourseBoundCiloQuestionBinding`, `CourseBoundEvaluationTarget`, `CourseBoundEvaluationExclusion`, `CentralDeployment`, and `EvaluationAssignment` unchanged; acceptance: one-to-one course-bound evaluation link, deployment relations, exclusion composite FKs, named relation strings, and SQL-only exclusion/deployment invariants remain unchanged; verify with `pnpm exec prisma validate --schema prisma`, and focused evaluation/deployment tests; commit: `refactor(prisma): split evaluation and deployment models`
- [x] 2.7 Add `prisma/models/responses.prisma` and move `ResponseStatus`, `DeploymentType`, `Response`, `QuantitativeResponseItem`, and `QualitativeResponseItem` unchanged; acceptance: response assignment uniqueness, respondent relation, item relations, and enum values remain unchanged; verify with `pnpm exec prisma validate --schema prisma` and focused response/analytics tests; commit: `refactor(prisma): split response models`

## 3. Tooling and Documentation Slice

- [x] 3.1 Update `scripts/create-supabase-migration.ts` and Prisma-consuming package scripts to pass `prisma` schema directory; generate the folder schema before `db:seed`; add focused tests for migration arguments and package scripts; acceptance: migration helper and package scripts load all fragments and never point only at `prisma/schema.prisma`; verify with focused tests, `pnpm postinstall`, and `pnpm supabase:migration:diff -- modularize_prisma_schema_check` in controlled environment; commit: `refactor(prisma): load schema directory for tooling`
- [x] 3.2 Update exact schema workflow references in `supabase/README.md` and `docs/cloie-techstack.md` to describe `prisma/schema.prisma` as entrypoint for a complete schema directory; acceptance: tracked docs no longer instruct maintainers to treat one file as complete and retain the no-Docker workflow; verify with targeted search; commit: `docs(prisma): document modular schema workflow`

## 4. Refactor Invariant Verification Slice

- [x] 4.1 Generate post-change Prisma Client and validate/format complete schema directory; acceptance: format, validation, and generation succeed with no duplicate or missing model/enum errors; verify with `pnpm exec prisma format --schema prisma`, `pnpm exec prisma validate --schema prisma`, and `pnpm exec prisma generate --schema prisma`; commit: `test(prisma): verify composed schema`
- [x] 4.2 Compare pre-change and post-change normalized from-empty SQL/DMMF/model metadata; acceptance: no unintended table, column, enum, index, constraint, default, mapping, foreign-key, relation-action, or UUID/native-type difference; no generated Supabase type edit; verify with temporary `prisma migrate diff` outputs and schema metadata comparison; commit: `test(prisma): compare schema refactor output`
- [x] 4.3 Audit all 45 `supabase/migrations/*.sql` files for unchanged SQL-only invariants and confirm no migration was created solely for file reorganization; acceptance: `NULLS NOT DISTINCT` indexes, partial unique indexes, CHECK constraints, roster/evaluation/readiness triggers, RLS, revokes, and composite migration assumptions remain present; verify with targeted migration searches and `git diff -- supabase/migrations src/types/supabase-database.ts prisma/seed.ts`; commit: `test(prisma): protect sql-only invariants`
- [x] 4.4 Run database-focused tests only against disposable database configuration with existing gate; acceptance: four gated invariant suites remain gated and pass when explicitly opted in, while default `pnpm test` does not write hosted Supabase; verify with `pnpm test`, then `RUN_DATABASE_INTEGRATION_TESTS=1 pnpm test:db` only when disposable `DATABASE_URL` is confirmed; commit: `test(prisma): verify database invariants`
- [x] 4.5 Run complete required verification and record results; acceptance: lint, unit tests, and production build pass; verify with `pnpm lint`, `pnpm test`, and `pnpm build`; commit: `test(prisma): verify schema modularization`

## 5. Approval and Handoff

- [x] 5.1 Review exact affected paths and confirm no implementation begins before OpenSpec approval; acceptance: proposal, design, spec, and tasks are internally consistent, dependency risk is explicit, and rollback is documented; verify with `openspec status --change modularize-prisma-schema` and `openspec validate --change modularize-prisma-schema`; commit: `docs(openspec): finalize prisma schema modularization plan`
- [x] 5.2 After explicit approval, convert dependency-ordered slices to GitHub tickets with `to-tickets`, including required skills and reference context per ticket, then implement through `implement` with `tdd`; acceptance: implementation does not start before approval and every ticket retains slice acceptance criteria, skills, references, and commands; verify through issue links and OpenSpec task updates; commit: `docs(prisma): track schema modularization slices`
