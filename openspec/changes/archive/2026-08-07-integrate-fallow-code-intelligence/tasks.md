## 1. Pinned Fallow Policy and Baseline Tooling

- [x] 1.1 Establish the project-local Fallow contract and the initial one-way seam policy. Scope: pin `fallow` to `2.54.3`; add `fallow:audit`, `fallow:dead-code`, `fallow:dupes`, `fallow:health`, `fallow:flags`, and `fallow:baseline` scripts; expand `.fallowrc.json` with documented `react-dom` handling, warning-only CSS resolver treatment, audit baseline locations, analysis paths, and the initial zones/rules. The policy must classify tests, routes/proxy, Server Actions, features, shared presentation, UI primitives, shared infrastructure, hooks, types, scripts, and Prisma; it may enforce only `ui-primitives -> shared` and `shared -> types`. Paths: `package.json`, `pnpm-lock.yaml` only if the exact package pin changes lock metadata, `.fallowrc.json`, `src/__tests__/config/fallow-config.test.ts` (new). Acceptance: `pnpm exec fallow dead-code --boundary-violations` produces zero current boundary findings; a fixture/config test demonstrates that a UI primitive-to-feature import and shared-to-route import are rejected while a Server Action-to-feature-service import and shared Supabase-to-generated-types import are permitted; `react-dom` does not produce a test-only-dependency finding; the valid `shadcn/tailwind.css` resolver mismatch is warning-only. Verify: `pnpm vitest run src/__tests__/config/fallow-config.test.ts`, `pnpm exec fallow config`, `pnpm exec fallow dead-code --boundary-violations --format json --quiet`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `build(fallow): define pinned analysis policy`.

- [x] 1.2 Add safe baseline-refresh tooling without generating an initial baseline from a dirty checkout. Scope: create `scripts/refresh-fallow-baselines.ts` that confirms the checkout is an up-to-date clean `main` worktree, runs the matching Fallow dead-code, health, and duplication `--save-baseline` commands into a staging directory, validates all three outputs, and publishes the complete validated generation as one directory swap (park current `fallow-baselines/` at `.fallow-baselines-previous`, rename the staged generation into place as the commit point, re-validate, then remove the parked generation) with rollback of every pre-commit failure and crash recovery on the next run; make the script fail when the ref, worktree, analyzer command, or output is invalid. This task creates baseline tooling only; it MUST NOT commit snapshots created from development work. Paths: `scripts/refresh-fallow-baselines.ts`, `package.json`, `src/__tests__/scripts/refresh-fallow-baselines.test.ts` (new), `.gitignore` only if needed to distinguish tracked baseline files from `/.fallow` cache and transaction artifact files. Acceptance: a clean-main fixture/test path produces all three baseline outputs; tracked/untracked worktree changes and a non-main ref are rejected before any baseline file is replaced; a second or third analyzer failure, malformed output, a pre-commit publish failure, or an interrupted transaction before the commit point preserves all prior final baseline bytes and removes temporary files; an interruption at or after the commit point is completed or restored by the next run once the crashed run's lock file is removed; a held lock refuses a concurrent or crashed refresh without modifying any generation; baseline output contains no environment values or application data. Verify: `pnpm vitest run src/__tests__/scripts/refresh-fallow-baselines.test.ts`, `pnpm fallow:baseline --help` or the script's documented dry-run/validation seam, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `build(fallow): add guarded baseline refresh`.

## 2. Clean-Main Baseline Bootstrap

- [x] 2.1 Generate and review the first committed identity baselines after tasks 1.1 and 1.2 are merged to `main`. Scope: from a clean, up-to-date `main` checkout, run `pnpm fallow:baseline`, inspect the dead-code, health, and duplication deltas, commit the three `fallow-baselines/*.json` files, and verify baseline identity matching with a controlled fixture or temporary Git worktree. The fixture must establish a known baseline finding, preserve that finding in a changed file, then add a distinct error-severity changed-file finding. This is a dependent operational slice; do not generate these files from an unrelated dirty feature branch. Paths: `fallow-baselines/dead-code.json`, `fallow-baselines/health.json`, `fallow-baselines/dupes.json`, `src/__tests__/scripts/fallow-baseline-audit.test.ts` (new). Acceptance: all three files are tracked, parse as Fallow 2.54.3 baseline outputs, correspond to the finalized `.fallowrc.json`; an unchanged-identity baseline finding is excluded; and a distinct unmatched error finding returns the native audit failure verdict. Verify: `git status --short` is clean before generation, `pnpm fallow:baseline`, `pnpm vitest run src/__tests__/scripts/fallow-baseline-audit.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `chore(fallow): baseline current code intelligence`.

## 3. Pull-Request Gate and Full-Report Workflow

- [x] 3.1 Add the least-privilege pull-request quality gate. Scope: create `.github/workflows/code-intelligence.yml` with `contents: read`, full Git checkout, the existing pinned pnpm/Node setup, a pull-request-to-`main` job that runs the project-local baseline-backed Fallow audit against `github.event.pull_request.base.sha` without `--fail-on-issues`, captures JSON and SARIF from separate Fallow runs because `--sarif-file` is a no-op in Fallow 2.54.3, uploads artifacts with `if: always()`, and propagates the saved audit result only after upload. Do not modify the existing lint/test/build workflow or add write permissions, PR comments, Code Scanning uploads, third-party Fallow actions, telemetry, or source fixes. Paths: new `.github/workflows/code-intelligence.yml`, `scripts/run-fallow-audit.ts` (new), `src/__tests__/scripts/run-fallow-audit.test.ts` (new), `src/__tests__/config/code-intelligence-workflow.test.ts` (new). Acceptance: the workflow rejects an absent base SHA, has `fetch-depth: 0`, uses `pnpm exec fallow`, passes all three committed baselines to the audit, never adds `--fail-on-issues` or `--ci`, and has only read-level repository permission. Parameterized runner tests use a controllable analyzer to prove warning-only exit `0` retains both artifacts and succeeds; unmatched error exit `1` uploads artifacts then fails; execution/configuration exit `2`, spawn failure, and unexpected status upload every available diagnostic then fail. Verify: `pnpm vitest run src/__tests__/scripts/run-fallow-audit.test.ts src/__tests__/config/code-intelligence-workflow.test.ts`, YAML parse validation, `pnpm exec tsx scripts/run-fallow-audit.ts HEAD~1`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `ci(fallow): gate pull request regressions`.

- [x] 3.2 Add non-blocking complete-codebase intelligence reporting. Scope: create `scripts/run-fallow-reports.ts` and a `fallow:reports` script, then extend the same workflow with push-to-`main`, weekly schedule, and `workflow_dispatch` report jobs. The runner invokes full dead-code, duplication, health/hotspot/target, feature-flag, and boundary inventories; captures JSON stdout; supplies companion SARIF output paths; accepts command exits `0` and `1` as report completion; and fails for exit `2`, spawn/signal failure, or another unexpected execution error after available artifacts upload. Paths: `scripts/run-fallow-reports.ts` (new), `package.json`, `.github/workflows/code-intelligence.yml`, `src/__tests__/scripts/run-fallow-reports.test.ts` (new), `src/__tests__/config/code-intelligence-workflow.test.ts`. Acceptance: parameterized runner tests cover Fallow command exits `0`, `1`, `2`, an unexpected exit status, and spawn failure; each test asserts available JSON/SARIF diagnostics are retained; scheduled, manual, and merged-main events invoke the complete reports; report commands do not run `fallow fix`, do not consume secrets, and upload results; the pull-request gate remains distinct and blocking only for unmatched error findings. Verify: `pnpm vitest run src/__tests__/scripts/run-fallow-reports.test.ts src/__tests__/config/code-intelligence-workflow.test.ts`, YAML parse validation, `pnpm fallow:reports`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `ci(fallow): publish codebase intelligence reports`.

## 4. Agent Workflow and Durable Operating Policy

- [x] 4.1 Configure the project-local Fallow MCP server and document the agent workflow. Scope: create root `opencode.json` with the documented schema and local `pnpm exec fallow-mcp` command; add Fallow to `docs/skills.md`; add a concise `AGENTS.md` code-intelligence section and `docs/agents/fallow.md` runbook covering project-local commands, trace-before-delete/refactor rules, dry-run-only automation, known framework/generated-code exceptions, baseline maintenance, artifact interpretation, and report-to-issue intake. Paths: new `opencode.json`, `AGENTS.md`, `docs/skills.md`, new `docs/agents/fallow.md`, `src/__tests__/config/opencode-fallow-mcp.test.ts` (new), `src/__tests__/config/fallow-agent-guidance.test.ts` (new). Acceptance: the OpenCode configuration validates against the published config schema; `fallow-mcp` initializes as version 2.54.3; documentation does not advertise commands unsupported by that version; agents are instructed to trace findings and never run `fix --yes` or baseline refresh unattended. Verify: MCP initialize handshake, `pnpm vitest run src/__tests__/config/opencode-fallow-mcp.test.ts src/__tests__/config/fallow-agent-guidance.test.ts`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `docs(fallow): establish agent analysis workflow`.

- [x] 4.2 Record the durable Fallow policy and future-remediation intake. Scope: add ADR 0011 describing the baseline-backed gate, narrow initial seams, report-only full scans, no telemetry/runtime collection/automatic mutation, and explicit policy-expansion requirement; document current top remediation candidates as evidence for focused follow-up issues without changing product modules or duplicating the scope of issues #167 and #174. Paths: new `docs/adr/0011-fallow-code-intelligence-policy.md`, `docs/agents/fallow.md`, `docs/skills.md` only if the catalog wording needs alignment. Acceptance: the ADR explains why generic architecture presets and unbaselined full-project gates are rejected; the operations document distinguishes analyzer evidence from approval to edit; future work is organized around traced dead code, duplicate modules, complexity hotspots, and dedicated seam designs. Verify: `openspec validate integrate-fallow-code-intelligence --strict`, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `docs(architecture): record fallow quality policy`.

## 5. Change Verification and Handoff

- [x] 5.1 Verify the integrated quality platform end to end after all preceding slices are present. Scope: validate all OpenSpec artifacts, project scripts, baseline matching, seam enforcement, workflow configuration, and MCP startup; run the complete project quality suite; capture the first report artifact/summary and create focused follow-up issues only for agreed remediation targets. Paths: `openspec/changes/integrate-fallow-code-intelligence/`, Fallow configuration/scripts/workflows/docs/tests from prior tasks, GitHub issues as needed. Acceptance: PR audit blocks a controlled unmatched fixture finding but not its baseline-matched equivalent; full reports remain non-blocking; initial seam violations are caught; all project checks pass; no database, auth, cache, or product behavior changes are present. Verify: `openspec validate integrate-fallow-code-intelligence --strict`, focused Fallow config/workflow/MCP tests, `pnpm lint`, `pnpm test`, `pnpm build`. Commit: `test(fallow): verify code intelligence integration`.
