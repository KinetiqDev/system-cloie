## 1. Responsive roster-management workspace

- [x] 1.1 Deliver one responsive `Manage roster` workspace with the existing CSV import and add-one-Student methods. Scope: modify `src/features/course-assignments/components/course-roster-management.tsx`; reuse `Dialog`, `Drawer`, `useMediaQuery`, `Tabs`, `WizardStepper`, current CSV parser/export helpers, and existing Server Actions; keep client state limited to overlay, method, file, phase, and session results. Acceptance: authorized mutable roster managers can open equivalent desktop Dialog and mobile Drawer methods; selected Program ID still reaches both existing actions; no new dependency, client fetch, cache, route, or authorization rule exists. Verify: focused roster component tests, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): add responsive roster management workspace`.

- [x] 1.2 Deliver resilient CSV flow inside management workspace. Scope: modify `src/features/course-assignments/components/course-roster-management.tsx` and `src/__tests__/components/course-assignments/course-roster-pages.test.tsx`; advance successful CSV imports from `Add members` to `Results`, preserve safe rows/summary/support reference/failed export, keep parser and action failures adjacent to input, reset state on close/reopen, and block closing while an import is pending. Acceptance: partial success remains visible and session-only; no completed membership write is reversed; reopening has no stale file, feedback, or results; add-one feedback stays direct. Verify: `pnpm vitest run src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): show roster import results in management workspace`.

## 2. Course roster detail hierarchy

- [x] 2.1 Deliver Course-assignment context, distinct KPI counts, and single management entry on roster detail. Scope: modify `src/features/course-assignments/components/course-roster-pages.tsx` and `src/__tests__/components/course-assignments/course-roster-pages.test.tsx`. Acceptance: detail shows Course code, title, Program, year level, Class section, Academic Period, lifecycle state, active-roster count, and current evaluation-eligible count before one management entry and member table; counts use existing prepared server data and tabular figures. Verify: `pnpm vitest run src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm lint`, `pnpm build`. Commit: `feat(course-rosters): clarify roster detail context and counts`.

- [x] 2.2 Preserve role, lifecycle, privacy, and review invariants while replacing old write blocks. Scope: modify `src/features/course-assignments/components/course-roster-pages.tsx` and `src/__tests__/components/course-assignments/course-roster-pages.test.tsx`. Acceptance: `FACULTY`, `SECRETARY`, `DEAN`, and selected-Program `PROGRAM_HEAD` retain existing action scope; unauthorized and read-only detail does not expose management; lifecycle banner, safe unavailable error, empty member view, filters, removed history, pagination, remove, and restore behavior remain unchanged. Verify: focused roster tests, `pnpm test`, `pnpm lint`, `pnpm build`. Commit: `test(course-rosters): protect redesigned roster detail invariants`.

## 3. Design-system and release verification

- [x] 3.1 Record every added or moved production roster component in design-system inventory only if implementation changes component paths. Scope: modify `src/features/design-system/data/production-surface-inventory.ts` only when needed. Acceptance: each affected production surface has exactly one disposition with task `15`; no unrelated inventory entry changes. Verify: inventory parity test or `pnpm test`, `pnpm lint`, `pnpm build`. Commit: `chore(design-system): inventory roster management surface`.

- [x] 3.2 Complete change-level regression and visual checks. Scope: implementation artifacts and affected roster tests. Acceptance: light/dark semantic tokens, desktop/mobile layout, keyboard focus, overlay focus restoration, touch targets, row-result readability, reduced-motion behavior, and safe error copy match `docs/design.md`; record #143 as remaining browser acceptance owner rather than claiming its E2E coverage. Verify: `pnpm vitest run src/__tests__/components/course-assignments/course-roster-pages.test.tsx`, `pnpm test`, `pnpm lint`, `pnpm build`. Commit: `test(course-rosters): verify management workspace redesign`.

Verification note: Component regression and semantic visual checks pass for the responsive Dialog/Drawer, focus restoration, pending state, result-table containment, reduced motion, and safe errors. Full Vitest passes with `--testTimeout=30000`; the default 5-second timeout is intermittently exceeded by the pre-existing design-system showcase test. Browser acceptance remains owned by #143, and deployment auth-boundary checks require a configured evidence URL.
